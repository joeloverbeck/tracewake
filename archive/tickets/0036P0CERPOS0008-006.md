# 0036P0CERPOS0008-006: P0-05 evidence — scheduler paths cannot emit primitive world actions from raw truth, routine labels, need thresholds, or fixture tables

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-05 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 5 (spec §7.5): scheduler paths must not synthesize primitive world actions — every scheduler-emitted event in the no-human corpus must be passive non-cognitive accounting, an ordinary actor decision transaction output, or a typed wait/stuck/failure record. This is the highest-risk seam for shortcut drift. This ticket classifies every scheduler-emitted event and gathers the negatives, then writes the P0-05 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-core/src/scheduler.rs` including the inline `no_human` module (`pub mod no_human` at `scheduler.rs:218`, `run_no_human_day` at `:344`), `need_accounting.rs`, `time.rs`, `agent/{transaction,no_human_surface}.rs`, `actions/{pipeline,report}.rs`, `events/{log,apply}.rs`.
2. Spec §7.5 names the corpus; the fixtures it hedges "if exercised" — `severe_safety_with_known_exit_produces_move_001`, `severe_safety_without_known_exit_waits_with_knowledge_blocker_001`, `sleep_spanning_window_boundary_charges_each_tick_once_001` — are all present at HEAD (confirmed this session) and folded into scope. `no_human_metrics_require_typed_responsible_layer_001` (added in this session's `/reassess-spec`) is also present.
3. Shared boundary under audit: the scheduler→decision-transaction→pipeline path. The audit classifies every scheduler-emitted no-human event into exactly one of three allowed categories with typed cause + single-charge proof where passive.
4. `INV-103` (the scheduler is not a cognition authority) and `INV-104` (routines/needs do not dispatch primitive actions directly) motivate this ticket; passive accounting must be typed and replayable, never a primitive action shortcut.
5. This ticket audits the scheduler/no-human, replay, and event-append surfaces as an evidence consumer. It runs the no-human positives, the single-charge/passive-accounting fixtures, and the scheduler negatives, recording that routine/need pressure prioritizes attention but never becomes a primitive action or hidden target, and that no-human metrics and hidden-truth rows stay observer-only and do not feed scheduler priority. It weakens no enforcement; a mutation that has the scheduler call action defs directly, emit primitives from need thresholds, use routine labels as actions, or read fixture tables as plan sources must be killed (recorded `fail` if surviving).

## Architecture Check

1. Classifying every scheduler-emitted event into the three allowed categories with typed cause is stronger than spot-checking a happy path: it closes the primitive-action-emission shortcut that needs both static and behavior-witness evidence (spec §11.1 #5).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-104` no direct primitive dispatch -> codebase grep-proof + replay check: `scheduler_cannot_rewrite_wait_reason_after_transaction_001`, `method_fallback_requires_new_trace_or_stuck_001` fail closed.
2. `INV-103` scheduler not cognition -> replay/golden-fixture check: replay reconstructs scheduler order, passive accounting, actor transaction outputs, and no-human metrics byte-identically within declared scope (`no_human_capstone`, `no_human_day_001`).
3. Passive single-charge accounting -> manual review: `wait_then_window_passive_charges_each_tick_once_001`, `sleep_spanning_window_boundary_charges_each_tick_once_001` charge once per tick.

## What to Change

### 1. Classify every scheduler-emitted no-human event

For the no-human corpus, classify each scheduler-emitted event as (1) passive non-cognitive accounting allowed by doctrine, with typed cause and single-charge proof; (2) ordinary actor decision transaction output with proposal/pipeline ancestry; or (3) modeled wait/stuck/failure record with typed reason.

### 2. Record provenance, debug-quarantine, and negatives

For each scheduler-initiated actor decision, show the sealed actor-known context and provenance frontier supplied to the transaction. Confirm no-human metrics and hidden-truth comparison rows are observer/debug-only and do not feed scheduler priority or actor decision selection. Map failures to typed diagnostics from {`scheduler`, `holder_known_context`, `candidate_generation`, `method_selection`, `local_planning`, `proposal_construction`, `action_validation`, `event_append`, `replay`}.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Validation-truth fallback boundary (P0-06, owned by `-007`).
- Typed-diagnostic standard generally (P0-09, owned by `-010`) beyond scheduler-layer diagnostics.
- Remediation of any scheduler shortcut found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `no_human_day_001`, `ordinary_workday_001`, `sleep_eat_work_001` and `cargo test -p tracewake-core --test no_human_capstone` — no-human ordinary interval through transaction/pipeline.
2. `wait_then_window_passive_charges_each_tick_once_001`, `sleep_spanning_window_boundary_charges_each_tick_once_001` — single-charge passive accounting.
3. Negatives fail closed: `scheduler_cannot_rewrite_wait_reason_after_transaction_001`, `no_human_unseen_workplace_assignment_does_not_plan_work_001`, `no_human_workplace_knowledge_requires_notice_event_001`, `no_human_known_workplace_requires_provenance_001`, `method_fallback_requires_new_trace_or_stuck_001`.
4. `no_human_metrics_require_typed_responsible_layer_001` — no-human metrics carry a typed responsible layer.

### Invariants

1. No scheduler path emits `eat`/`sleep`/`work`/`move`/`take`/`inspect`/`open`/`check` (or any primitive) directly from truth, routine labels, need thresholds, or fixture tables (`INV-103`/`INV-104`).
2. Replay reconstructs scheduler order, passive accounting, and transaction outputs within declared fingerprint scope (`INV-018`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing no-human/scheduler fixtures and capstone; the captured event classification is the deliverable.`

### Commands

1. `cargo test -p tracewake-core --test no_human_capstone`
2. `cargo test -p tracewake-content --test golden_fixtures_run` (exercises the no-human, single-charge, and scheduler-negative fixtures)
3. `cargo test -p tracewake-core --test anti_regression_guards` (no-direct-dispatch / scheduler static guards)

## Outcome

Completed: 2026-06-16

Filled the P0-05 section of `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` with certifying evidence IDs:

1. `0036-P0-05-NOHUMAN-CAPSTONE-ANCESTRY`
2. `0036-P0-05-PASSIVE-ACCOUNTING-SINGLE-CHARGE`
3. `0036-P0-05-SCHEDULER-SHORTCUT-NEGATIVES`

The current user request to implement the series satisfied the ticket's evidence-only approval to proceed. The captured witnesses classify scheduler/no-human output into allowed categories: no-human markers, passive typed accounting, ordinary transaction/pipeline output with proposal/process ancestry, and modeled wait/stuck/failure records. Static and behavior negatives cover direct primitive dispatch, routine-family-to-action mappings, direct need/routine proposal bypasses, fabricated projection inputs, post-cognition proposal rewriting, and no-human metrics/display rows feeding actor cognition.

Verification run:

1. `cargo test -p tracewake-core --test no_human_capstone` - passed, 1 test.
2. `cargo test -p tracewake-content --test golden_fixtures_run` - passed, 40 tests.
3. `cargo test -p tracewake-core --test anti_regression_guards` - passed, 80 tests.

No code changes were required. The full configured mutation baseline remains pending from `0036P0CERPOS0008-001` and is not counted as P0-05 pass evidence.
