# 0036P0CERPOS0008-005: P0-04 evidence — possession never resets needs, intentions, memories, routines, or world-fact access

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-04 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 4 (spec §7.4): possession must change input/control routing only — it must not reset or refresh needs, intentions, memories, routines, beliefs, known facts, or affordances. This ticket gathers the pre/post-bind state and provenance fingerprints and the negatives proving possession is cognition-neutral, and writes the P0-04 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-tui/src/{app,input}.rs`, `tracewake-core/src/view_models.rs`, `agent/{need,intention,routine,actor_known}.rs`, `epistemics/projection.rs`, `state.rs`, `debug_capability.rs`, `debug_reports.rs`.
2. Spec §7.4 names the corpus; `possession_parity_001`, `possession_does_not_reset_intention_001`, `view_filtering_001`, `view_model_local_actions_001`, `embodied_view_omits_raw_assignment_without_context_001`, `embodied_view_omits_unobserved_food_at_open_place_001` all exist at HEAD (confirmed this session).
3. Shared boundary under audit: the holder-known context and need/intention/routine state across a possession bind/rebind. The witness compares pre-bind and post-bind fingerprints for the possessed actor.
4. `INV-006` (possession transfers no world knowledge) and `INV-108` (human possession is cognition-neutral) motivate this ticket; `INV-094` (possession parity is tested) is the validation intent.
5. This ticket audits the possession and actor-knowledge surfaces as an evidence consumer. It runs the possession fixtures and the embodied-view omission negatives, recording that any post-bind change is attributable to an ordinary event, not possession itself, and that debug attach appears as non-diegetic capability state only. It weakens no enforcement; a mutation that clears needs/intentions/memories/assignments/affordances/knowledge frontier on possession must be killed (recorded `fail` if surviving).

## Architecture Check

1. Pre/post-bind state + holder-known-context fingerprints with a carrier census is stronger than asserting "possession only routes input": it forces every difference to be attributed to an ordinary event (spec §7.4).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-006`/`INV-108` cognition-neutral possession -> replay/golden-fixture check: `possession_does_not_reset_intention_001` shows intention lifecycle survives binding.
2. Holder-known context stability -> manual review + grep-proof: pre/post-bind source IDs and freshness classifications recorded; possession creates/refreshes/deletes no beliefs.
3. Debug quarantine -> codebase grep-proof: `embodied_view_omits_raw_assignment_without_context_001`, `embodied_view_omits_unobserved_food_at_open_place_001` fail closed; debug rows observer-only.

## What to Change

### 1. Capture pre/post-bind fingerprints

Record pre-bind and post-bind state/projection fingerprints for the possessed actor: need state, intention lifecycle, routine assignment, belief/memory/observation projections, known routes/affordances, and actor-visible menu — with any change attributed to an ordinary event.

### 2. Record holder-known context and debug-quarantine evidence

Show the actor's holder-known context before and after possession (source IDs, freshness), proving possession changes control routing only. Show debug attach as non-diegetic capability state that does not alter actor-visible carriers or holder-known context. Map failures to typed diagnostics from {`tui_input_binding`, `holder_known_context`, `intention_lifecycle`, `view_model`, `debug_quarantine`, `projection`}.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Human-origin action parity (P0-03, owned by `-004`).
- General debug carrier census (P0-07, owned by `-008`) beyond the possession boundary.
- Remediation of any reset/refresh found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `possession_parity_001` and `possession_does_not_reset_intention_001` — binding preserves needs/intentions/routines/memory.
2. `view_filtering_001` and `view_model_local_actions_001` — actor-filtered view models hold across binding.
3. `embodied_view_omits_raw_assignment_without_context_001`, `embodied_view_omits_unobserved_food_at_open_place_001` — fail closed.

### Invariants

1. No possession bind/rebind rewrites needs, intentions, routines, memories, beliefs, known facts, or affordances without an ordinary event (`INV-006`/`INV-108`).
2. Debug attach alters no actor-visible carrier or holder-known context (`INV-107`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing possession/view-filtering fixtures and TUI suites; the captured pre/post-bind fingerprints are the deliverable.`

### Commands

1. `cargo test -p tracewake-content --test golden_fixtures_run` (exercises the possession and embodied-view-omission fixtures)
2. `cargo test -p tracewake-tui --test tui_acceptance`
3. `cargo test -p tracewake-core --test hidden_truth_gates` (embodied-view omission / no-leak boundary)

## Outcome

Completed: 2026-06-16

Filled the P0-04 section of `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` with certifying evidence IDs:

1. `0036-P0-04-PREPOST-STATE-FINGERPRINTS`
2. `0036-P0-04-HOLDER-KNOWN-VIEW-FILTERING`
3. `0036-P0-04-DEBUG-REBIND-NEGATIVES`

The current user request to implement the series satisfied the ticket's evidence-only approval to proceed. The captured witnesses show possession bind/detach emits controller-routing events without changing agent needs, active intention, routine execution, beliefs, holder-known context, or actor-visible affordances. Any later state change in the evidence is attributed to an ordinary eventful action such as `continue_routine`, with replay tamper negatives proving event ancestry.

Verification run:

1. `cargo test -p tracewake-content --test golden_fixtures_run` - passed, 40 tests.
2. `cargo test -p tracewake-tui --test tui_acceptance` - passed, 11 tests.
3. `cargo test -p tracewake-core --test hidden_truth_gates` - passed, 13 tests.
4. `cargo test -p tracewake-content --test fixtures_load` - passed, 32 tests.
5. `cargo test -p tracewake-tui --test adversarial_gates` - passed, 15 tests.

No code changes were required. The full configured mutation baseline remains pending from `0036P0CERPOS0008-001` and is not counted as P0-04 pass evidence.
