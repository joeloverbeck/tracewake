# 0058MUTWIT-001: Reproduce and close the 0058 embodied-seam mutation survivors

**Status**: COMPLETED
**Priority**: MEDIUM
**Effort**: Small
**Engine Changes**: None — test-only. Adds behavioral witnesses (if needed) that kill surviving `cargo-mutants` mutants on `crates/tracewake-core/src/runtime/session.rs`; no production logic changes. May resolve to a recorded no-op disposition if the survivors do not reproduce under full-workspace scope.
**Deps**: None. Closes the mutation debt recorded open by `archive/reports/0058_embodied_routine_continuation_foundational_alignment_acceptance.md` §E-007. Independent of the planned 0059 autonomous-derivation hardening spec (different seam).

## Problem

The 0058 acceptance artifact (`archive/reports/0058_embodied_routine_continuation_foundational_alignment_acceptance.md`, §E-007) closed the 0058 series with **survivorful** focused mutation: its `--iterate` run left **7 missed mutants**, six in the embodied seam plus one adjacent, explicitly recorded as "not certified closed by this artifact":

- `crates/tracewake-core/src/runtime/session.rs:716` — `.find(|event| event.event_id == event_id)` (push the ordinary follow-on event into the receipt's `appended_events`).
- `crates/tracewake-core/src/runtime/session.rs:741` — `.find(|event| event.event_id == trace_event_id)` (push the decision-trace event into `appended_events`).
- `crates/tracewake-core/src/runtime/session.rs:1147` — `.filter(|execution| &execution.actor_id == actor_id)` in `embodied_routine_window_family` (the OQ1-fix execution scan: actor-id filter).
- `crates/tracewake-core/src/runtime/session.rs:1149` — `.filter(|execution| !execution.step_status.is_resolved())` (resolved-execution filter).
- `crates/tracewake-core/src/runtime/session.rs:1154` — `&& !actor_known_context.known_workplaces()…` (the `WorkBlock→GoToWork` refinement guard).
- `crates/tracewake-core/src/runtime/session.rs:1157` — `.any(|place_id| place_id == actor_known_context.current_place_id())` (the workplace-match predicate driving the refinement).
- `crates/tracewake-core/src/replay/rebuild.rs:159` — recorded as **outside the 0058 embodied seam** (see Out of Scope).

This ticket closes that recorded test debt honestly: first **reproduce** the survivors under the correct (full-workspace) test scope, then add killing witnesses only for those that genuinely survive.

## Assumption Reassessment (2026-06-30)

1. **The cited lines exist and are unchanged at HEAD.** `git diff --stat 2d98a221b547af8b8b687c3a9e36143f2c7cbb73 dffeefa -- crates/tracewake-core/src/runtime/session.rs crates/tracewake-core/src/replay/rebuild.rs` is empty — `session.rs` is byte-identical between 0058's implementation commit `2d98a22` and current HEAD `dffeefa`, so the §E-007 line numbers (716, 741, 1147, 1149, 1154, 1157) remain valid. Verified by direct read: 716/741 are the two `.find(|event| event.event_id == …)` receipt-append lookups; 1147/1149 are the actor-id and `is_resolved` filters in `embodied_routine_window_family`; 1154/1157 are the `WorkBlock`-refinement guard and its `current_place_id()` match. **Named symbols are authoritative; re-confirm at the implementation commit per the 0058 spec's source-discipline note.**

2. **Load-bearing discrepancy — §E-007 used a narrowed test selection that the standing perimeter does not.** The §E-007 commands restricted `cargo-mutants`' test command to tiny subsets: the first run with `-- -p tracewake-core --test anti_regression_guards guard_0058`, the `--iterate` run with `-- -p tracewake-core embodied_continue`. The standing perimeter `.cargo/mutants.toml` instead sets `test_workspace = true` (full `cargo test --workspace`), and the CI `mutation in-diff (lock layer)` gate runs against that full suite with an **empty** `.cargo/mutants-baseline-misses.txt` (zero-floor). Because `crates/tracewake-core/src/runtime/**` is inside `examine_globs` (`.cargo/mutants.toml:13`) and 0058 nonetheless **merged green through the PR-blocking in-diff gate**, the full-workspace suite very likely already kills these mutants. The §E-007 "survivorful" count is therefore suspected to be an artifact of the narrowed manual campaign's test scope, **not** a confirmed production-coverage gap. This reframes the ticket: the first task is reproduction under correct scope, not blind witness-writing. (Mirrors the brainstorm triage's remediation-feasibility caution: a recorded survivor is a hypothesis until reproduced.)

3. **Invariants under audit (the witnesses, if any, lock existing behavior — no production change).**
   - **INV-094 / INV-108** (possession parity; possession input-only): 1147/1149/1154/1157 are the active-intention-authority execution scan and workplace refinement that the 0058 OQ1 fix established; a witness must prove an inverted filter/guard produces an observably wrong routine family.
   - **INV-104** (routines route through the transaction/resolver, not environmental shortcuts): the filter/refinement is exactly the no-shortcut logic.
   - **INV-018 / INV-092** (deterministic replay is foundational/tested): 716/741 govern which events reconstruct into the follow-on receipt's `appended_events`; a witness must assert the specific event ids/content land in the receipt so an inverted/`.find`-dropped lookup is caught.
   - **INV-098** (feature acceptance is harsh): closing recorded mutation debt with behavioral proof is the harsh-acceptance follow-through 0058 deferred.

4. **Enforcement surface / no weakening.** Witnesses are test-only and must not edit the gate, the perimeter, or grow `.cargo/mutants-baseline-misses.txt` (zero-floor must stay zero). No epistemic-leakage or replay-determinism weakening; 1147/1149/1154/1157 already passed 0058's hidden-truth audits, and the witnesses only tighten output assertions.

5. **Adjacent contradiction classification.** §E-007's survivor count predates this ticket and was left open deliberately ("not certified closed") because 0058 used a *scoped* focused-mutation discipline (spec §5.3/§6.4), not a standing campaign. That is recorded debt this ticket remediates — not a separate bug. The narrowed-scope nature of the §E-007 commands (Assumption 2) is the corrected understanding, not a new defect.

6. **The existing sibling witnesses do not kill the filter mutants.** `embodied_routine_window_family_filters_executions_by_actor` and `embodied_routine_window_family_ignores_resolved_executions` (both in `session.rs`, preserved by 0058 ticket 001) exercise the 1147/1149 filters but evidently leave the mutants alive under the narrowed campaign — confirm whether they kill under full scope (Assumption 2); if not, strengthen their fixtures to make a removed filter change the *returned family*, rather than adding new mutant-shaped private tests.

## Architecture Check

1. **Reproduce-before-witness is cleaner than blind witness-writing.** Adding six tests for mutants that the full suite already kills would bloat the suite with redundant, mutant-shaped assertions (exactly what `docs/2-execution/10_*` warns against). Gating the work on a correctly-scoped reproduction means we add only the witnesses that close a *real* gap, and otherwise record a precise no-op disposition.
2. **No backwards-compatibility aliasing/shims** are introduced — test-only change (or no change). Witnesses, if added, prefer strengthening existing public-behavior tests over new private tests.

## Verification Layers

1. INV-094/INV-104 (possession parity / no routine shortcut) -> replay/golden-fixture check + manual review: an adversarial actor-known fixture (other-actor execution, resolved execution, at/not-at workplace) makes a mutated 1147/1149/1154/1157 return a different `RoutineFamily`, killing the mutant via observable output.
2. INV-018/INV-092 (deterministic replay) -> replay/golden-fixture check: a behavioral assertion that the follow-on receipt's `appended_events` contains the exact ordinary follow-on and decision-trace event ids kills the 716/741 `.find` mutants.
3. INV-098 (harsh acceptance) -> invariants alignment check + the focused-mutation command in Acceptance Criteria: the targeted `cargo-mutants` run over the six lines reports caught/equivalent for each, with honest disposition of any equivalent/unviable mutant.
4. Single-seam ticket: all layers map to `crates/tracewake-core/src/runtime/session.rs`; the additional-layer mapping above is the per-invariant proof split, not collapsed into one generic "review".

## What to Change

### 1. Reproduce the survivors under full-workspace scope (gating step)

Run focused `cargo-mutants` over the six `session.rs` lines using the **standing** config (full-workspace tests), not §E-007's narrowed `-- -p tracewake-core embodied_continue` selection. For any mutant the full suite already kills, record it as a narrowed-scope artifact and add no test.

### 2. Add killing witnesses only for genuine survivors

For each mutant that genuinely survives the full suite, strengthen the highest-level existing behavioral test (preferring `embodied_routine_window_family_*` unit witnesses for 1147/1149/1154/1157 and an embodied follow-on receipt test for 716/741) so a mutated predicate changes observable output. Disposition any equivalent/unviable mutant honestly.

## Files to Touch

- `crates/tracewake-core/src/runtime/session.rs` (modify — only if survivors persist; strengthen the in-module `#[cfg(test)]` `embodied_routine_window_family_*` witnesses and/or follow-on receipt assertions)
- `crates/tracewake-core/tests/embodied_autonomous_parity.rs` (modify — only if a receipt/append witness fits better as an integration test)

## Out of Scope

- `crates/tracewake-core/src/replay/rebuild.rs:159` — §E-007 records it as outside the embodied seam; its origin (whether a real `replay/**` gap or another narrowed-scope artifact) is a separate investigation, not this ticket's embodied-seam debt. Flagged for a follow-up if it reproduces under full scope.
- The planned 0059 autonomous-derivation hardening spec (`scheduler.rs` derivation) — different seam.
- Any change to the CI mutation gate, `.cargo/mutants.toml` perimeter, or `.cargo/mutants-baseline-misses.txt` (zero-floor stays zero).
- A full standing mutation campaign (the 0058 scoped-focused discipline stands).

## Acceptance Criteria

### Tests That Must Pass

1. The four local gates: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo build --workspace --all-targets --locked`; `cargo test --workspace`.
2. Focused mutation over the six lines reports **caught** (or documented equivalent/unviable) for each, under full-workspace test scope:
   `cargo mutants --package tracewake-core --file crates/tracewake-core/src/runtime/session.rs --re 'embodied_routine_window_family|run_embodied_continue_routine_follow_on' --baseline skip -j 2 --timeout 120`
   (no narrowing `-- -p … embodied_continue`; the standing `.cargo/mutants.toml` supplies `test_workspace = true`).
3. The CI `mutation in-diff (lock layer)` gate remains green with `.cargo/mutants-baseline-misses.txt` unchanged (empty).

### Invariants

1. No production logic changes; `embodied_routine_window_family` and `run_embodied_continue_routine_follow_on` behavior is identical pre/post (grep-proof: diff shows test-only changes).
2. The zero-floor mutation baseline (`.cargo/mutants-baseline-misses.txt` empty) is preserved.

## Test Plan

### New/Modified Tests

1. `crates/tracewake-core/src/runtime/session.rs` (`#[cfg(test)]`) — strengthen `embodied_routine_window_family_filters_executions_by_actor` / `_ignores_resolved_executions` so a removed actor-id/`is_resolved` filter (1147/1149) changes the returned family; add a workplace at/not-at adversary for 1154/1157 — **only if those mutants survive full-scope reproduction.**
2. An embodied follow-on receipt assertion (in-module or `tests/embodied_autonomous_parity.rs`) that `appended_events` contains the exact ordinary follow-on and decision-trace event ids — **only if 716/741 survive.**
3. `None — if full-scope reproduction kills all six, no tests change; record the narrowed-scope-artifact disposition in the implementation note and close.`

### Commands

1. `cargo mutants --package tracewake-core --file crates/tracewake-core/src/runtime/session.rs --re 'embodied_routine_window_family|run_embodied_continue_routine_follow_on' --baseline skip -j 2 --timeout 120` (targeted reproduction + kill proof, full-workspace tests)
2. `cargo test --workspace` (full-pipeline)
3. The narrowed `-- -p tracewake-core embodied_continue` selection from §E-007 is **not** the verification boundary here — it is the suspected source of the false survivors; the standing full-workspace scope is the correct boundary.

## Outcome

Completed: 2026-06-30

This ticket closed as a recorded no-op disposition. The standing full-workspace
mutation scope did not reproduce the six 0058 embodied-seam survivors as
survivors, so no new witness tests and no production changes were added.

Verification performed:

- Denominator check:
  `cargo mutants --package tracewake-core --file crates/tracewake-core/src/runtime/session.rs --re 'embodied_routine_window_family|run_embodied_continue_routine_follow_on' --baseline skip -j 2 --timeout 120 --list`
  selected 16 mutants under the checked-in standing config. The selected set
  included the six owned 0058 session-line mutants, additional same-function
  session mutants, and the known out-of-scope `replay/rebuild.rs:159` mutant
  because `.cargo/mutants.toml` contributes the standing perimeter.
- Focused full-workspace mutation proof:
  `cargo mutants --package tracewake-core --file crates/tracewake-core/src/runtime/session.rs --re 'embodied_routine_window_family|run_embodied_continue_routine_follow_on' --baseline skip -j 2 --timeout 120`
  completed with 16 tested, 14 caught, 2 unviable, 0 missed, and 0 timeouts.
  `mutants.out/missed.txt` and `mutants.out/timeout.txt` were empty.
- The six owned 0058 session-line mutants at `session.rs:716`, `session.rs:741`,
  `session.rs:1147`, `session.rs:1149`, `session.rs:1154`, and
  `session.rs:1157` were caught under the standing full-workspace scope.
- The selected but out-of-scope `replay/rebuild.rs:159` mutant was also caught;
  it remains outside this ticket's ownership boundary.
- The two unviable mutants were default-return replacements at
  `session.rs:546` and `session.rs:1141`; neither was one of the six recorded
  0058 owned survivors.
- `.cargo/mutants-baseline-misses.txt` remained empty (`0` bytes), and no
  production, test, workflow, or mutation-baseline diff was introduced. Because
  this closeout introduced no guarded production diff, the CI
  `mutation in-diff (lock layer)` gate would have no production mutant to run
  for this ticket-local change; the local zero-floor evidence remains intact.

Deviations from the original plan:

- No killing witnesses were added because no owned survivor reproduced under
  the correct full-workspace scope.
- The required command's standing-config selection was broader than the six
  lines named in the ticket. The broader selected mutants were classified
  separately rather than counted as additional owned 0058 debt.

Transient evidence:

- `mutants.out/` was used as local generated evidence only and was not made a
  tracked artifact. Scratch cleanup was not run during ticket closeout.
