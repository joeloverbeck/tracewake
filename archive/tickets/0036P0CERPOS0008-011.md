# 0036P0CERPOS0008-011: P0-10 evidence — archived specs and tickets are cited only as history

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-10 section of the acceptance artifact from existing tests/docs
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 10 (spec §7.10): archived specs and tickets may be cited only as history — never as a live gate pass. This line protects all other gates from historical-label overclaim. This ticket audits the wording/status surfaces and confirms live evidence comes from current exact-commit code, then writes the P0-10 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `docs/4-specs/SPEC_LEDGER.md`, `docs/4-specs/README.md`, `docs/3-reference/{00_REFERENCE_INDEX_AND_REVIEW_CHECKLIST,01_DESIGN_RISK_REGISTER}.md`, `archive/specs/0025_PHASE_3A_META_WITNESS_…_HARDENING_SPEC.md`, `archive/specs/0014_PHASE_3A_ORDINARY_LIFE_…_HARDENING_SPEC.md`, `crates/tracewake-core/tests/acceptance_artifact_wording.rs`, `crates/tracewake-core/tests/doc_invariant_references.rs`.
2. Spec §7.10 names the corpus; `acceptance_artifact_wording.rs` and `doc_invariant_references.rs` (both hedged "if present") are present at HEAD (confirmed this session). The `SPEC_LEDGER.md` confirms archived specs `0005`–`0035` are historical-evidence/`P0-CERT not applicable` rows and names `P0-CERT` as the next audit with no `0036` entry yet.
3. Shared boundary under audit: the evidence-status discipline (§8.2) for archive references — archive history supplies no event/replay/projection/provenance/debug evidence; any behavior a historical fixture/report points to must be reproduced from current exact-commit code with the live witness cited separately.
4. `INV-022` (raw prose is not authoritative state) and the doc-pack authority order / replacement doctrine (`docs/README.md`; design-risk-register relapse "treating archived specs as certification") motivate this ticket.
5. This ticket audits the documentation-status / wording surface. It runs `acceptance_artifact_wording.rs` and `doc_invariant_references.rs` (linters preventing stale gate-code/spec-ledger wording) and confirms no archive citation substitutes for live event/replay/provenance/debug/diagnostic evidence. It introduces no enforcement and weakens none; this is the relapse guard against label-implied pass (spec §11.1 #2).

## Architecture Check

1. Auditing wording/status linters plus requiring live reproduction of any archive-pointed behavior is stronger than trusting an archived acceptance report: it resists the label-implied-pass relapse (spec §11.1 #2).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. Replacement doctrine -> manual review + invariants alignment check: archive references carry status `historical`; none is counted as `pass`.
2. Wording discipline -> codebase grep-proof: `acceptance_artifact_wording.rs`, `doc_invariant_references.rs` reject stale gate-code / spec-ledger wording and archive-as-live-certification phrasing.
3. `INV-022` prose-not-authoritative -> manual review: any behavior an archived fixture/report points to is reproduced from current code with the live witness cited separately.

## What to Change

### 1. Audit archive-citation status

Record that every archive/spec/ticket reference in the acceptance artifact carries status `historical` only, and that the generated artifact's `pending-historical` and `certification-use` sections classify archive references correctly. Confirm no archive citation substitutes for event/replay/provenance/debug/diagnostic evidence.

### 2. Confirm live reproduction of historical behaviors

Where a historical fixture or report points to a behavior, show the implementing session reproduced it from current exact-commit code and current acceptance commands, citing the live witness separately. Map failures to typed diagnostics from {`doctrine`, `documentation status`, `test_oracle`}, plus the underlying implementation layer if an archive claim masks a real code failure.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- The `docs/4-specs/SPEC_LEDGER.md` row for spec 0036 and the `archive/specs/` move — deferred to spec acceptance/archival (a Step 6 cross-spec follow-up), not ticketed here.
- Remediation of any archive-as-live-certification wording found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `cargo test -p tracewake-core --test acceptance_artifact_wording` — archive references classified as historical; no stale gate-code wording.
2. `cargo test -p tracewake-core --test doc_invariant_references` — doc/invariant/spec-ledger references resolve and are not stale.
3. Manual review confirms no acceptance-artifact section, test name, report, or doc treats an archived accepted spec/ticket (`0005`–`0008` or any ticket) as a live gate pass.

### Invariants

1. No archived accepted spec/ticket is treated as a live `P0-CERT` gate pass (replacement doctrine).
2. Live gate evidence derives from current exact-commit code and acceptance commands, never from archive history (`INV-022`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing wording/doc-reference linters and the captured archive-status audit is the deliverable.`

### Commands

1. `cargo test -p tracewake-core --test acceptance_artifact_wording`
2. `cargo test -p tracewake-core --test doc_invariant_references`
3. `grep -nE "0005|0006|0007|0008" archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (confirm any archive reference is in a historical-status context, not a pass claim)

## Outcome

Completed: 2026-06-16

P0-10 is recorded as `pass` in
`archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md`
with evidence IDs:

1. `0036-P0-10-WORDING-LINTERS`
2. `0036-P0-10-ARCHIVE-STATUS-AUDIT`
3. `0036-P0-10-LIVE-REPRODUCTION-DISCIPLINE`

The report records that archive/spec/ticket evidence remains historical context
only and is not counted as live P0-CERT pass evidence. The artifact's pass rows
cite current-session commands, tests, scans, or direct report evidence; historical
references appear only in status/handling text and cannot substitute for
event/replay/provenance/debug/diagnostic evidence. The final artifact verdict
remains `<pending>` until the capstone ticket renders the final outcome.

Verification:

1. `cargo test -p tracewake-core --test acceptance_artifact_wording` - passed 4
   tests.
2. `cargo test -p tracewake-core --test doc_invariant_references` - passed 4
   tests.
3. `grep -nE "0005|0006|0007|0008" archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md`
   returned only the current post-0008/spec/ticket references and pending
   artifact context, not archived historical specs being counted as pass.
4. Supporting `rg` archive/history/status scan over the acceptance artifact,
   `docs/4-specs/SPEC_LEDGER.md`,
   `docs/3-reference/00_REFERENCE_INDEX_AND_REVIEW_CHECKLIST.md`, and
   `docs/3-reference/01_DESIGN_RISK_REGISTER.md` confirmed archived specs are
   documented as historical or scoped, not live full-project certification.

No engine code changed. The full mutation baseline remains pending from
`0036P0CERPOS0008-001` and is not converted to pass by archived history.
