# 0036P0CERPOS0008-010: P0-09 evidence — failure diagnostics name the responsible layer

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-09 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 9 (spec §7.9): failure diagnostics must name the responsible layer (authoring, schema validation, actor-known context, candidate generation, planning, proposal construction, scheduling, action validation, event application, projection, TUI rendering, replay, debug quarantine) using typed/structurally-inspectable records, not display strings. This ticket audits the diagnostic substrate and writes the P0-09 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-core/src/agent/trace.rs`, `actions/report.rs`, `events/apply.rs`, `replay/{rebuild,report}.rs`, `tracewake-content/src/validate.rs`, `controller.rs`, `scheduler.rs`, `tracewake-tui/src/render.rs`, `debug_reports.rs`.
2. Spec §7.9 names the corpus; `routine_blocked_diagnostic_001` and `work_block_failed_then_sleep_succeeds_001` (both hedged "if exercised") are present at HEAD (confirmed this session), as are `method_fallback_requires_new_trace_or_stuck_001` and `forbidden_provenance_input_fails_closed_001`.
3. Shared boundary under audit: the typed responsible-layer diagnostic vocabulary. The audit confirms the artifact uses the live layer names consistently: `doctrine`, `content_schema`, `content_validation`, `fixture_contract`, `holder_known_context`, `candidate_generation`, `intention_lifecycle`, `method_selection`, `local_planning`, `proposal_construction`, `scheduler`, `action_validation`, `event_append`, `event_application`, `projection`, `replay`, `view_model`, `debug_quarantine`, `tui_input_binding`, `test_oracle` (all verified as live doctrine vocabulary this session — spec mints none).
4. `INV-105` (actor decision traces are authoritative diagnostic data; display strings must not be the authoritative substrate) motivates this ticket.
5. This ticket audits the diagnostic and replay surfaces as an evidence consumer. It runs the diagnostic positives and the string-typed-diagnostic negatives, recording that post-proposal failures carry event/proposal/projection/checksum/replay IDs and pre-proposal failures record context/candidate/planning/source-data IDs. It weakens no enforcement; a mutation replacing typed records with display strings/substrings/`Err(String)`, or reporting the wrong responsible layer, must be killed (recorded `fail` if surviving).

## Architecture Check

1. Auditing diagnostics as typed records with layer + expected/actual source is stronger than scanning for error text: it resists the string-typed-diagnostic relapse where display labels masquerade as typed layer names (spec §11.1 #4).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-105` typed diagnostics -> codebase grep-proof + manual review: diagnostics in `agent/trace.rs`/`actions/report.rs`/`replay/report.rs` are typed/structurally inspectable; display strings only summarize.
2. Layer-attribution correctness -> manual review: hidden-truth/scheduler/validation/replay/projection/debug-quarantine failures name the correct layer; `forbidden_provenance_input_fails_closed_001` names `holder_known_context`.
3. Banned-word independence -> schema validation: `hidden_truth_audit_rejects_typed_unproven_fact_without_banned_words_001` proves typed checks, not substring scanning.

## What to Change

### 1. Audit diagnostic typing and layer attribution

Record that, where failure arises after proposal/event/replay seams, diagnostics carry event/proposal/projection/checksum/replay identifiers; where failure arises before proposal, diagnostics record context/candidate/planning/source-data identifiers available at that layer. For actor-known failures, name expected input source, actual input source, holder-known context ID/hash/frontier, missing/forbidden/stale/wrong-kind provenance, and any actor-visible output.

### 2. Record the responsible-layer vocabulary and negatives

Confirm the acceptance artifact uses the live responsible-layer names consistently and that diagnostics distinguish actor-visible from debug-only output (naming debug leakage/exclusion where relevant). Show negatives fail closed: tests/mutants replacing typed records with display strings/substrings/`Err(String)`, and hidden-truth audit negatives without banned-word dependence.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Per-gate failure witnesses owned by `-002`…`-009` and `-011`; this gate audits the diagnostic substrate and layer naming, citing those gates' diagnostics.
- Remediation of any string-typed/wrong-layer diagnostic found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `routine_blocked_diagnostic_001`, `method_fallback_requires_new_trace_or_stuck_001`, `work_block_failed_then_sleep_succeeds_001` — typed stuck/blocked/failure diagnostics.
2. `cargo test -p tracewake-content --test forbidden_content` and `cargo test -p tracewake-core --test event_schema_replay_gates` — content-validation and replay-divergence diagnostics name their layer.
3. `forbidden_provenance_input_fails_closed_001` and the hidden-truth audit negative (no banned-word dependence) name the correct responsible layer.

### Invariants

1. No failure artifact merely says a test failed, relies on display text, omits responsible layer / expected-vs-actual source, or cannot distinguish hidden-truth-excluded from hidden-truth-leaked (`INV-105`).
2. The artifact's responsible-layer names match live doctrine vocabulary; none is newly minted.

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing diagnostic/validation/replay tests and the captured layer-attribution audit is the deliverable.`

### Commands

1. `cargo test -p tracewake-content --test forbidden_content`
2. `cargo test -p tracewake-core --test event_schema_replay_gates && cargo test -p tracewake-core --test no_human_capstone`
3. `cargo test -p tracewake-core --test hidden_truth_gates` (typed-provenance, banned-word-independent diagnostics)

## Outcome

Completed: 2026-06-16

P0-09 is recorded as `pass` in
`archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md`
with evidence IDs:

1. `0036-P0-09-TYPED-DIAGNOSTIC-SUBSTRATE`
2. `0036-P0-09-REPLAY-CONTENT-LAYER-DIAGNOSTICS`
3. `0036-P0-09-HIDDEN-TRUTH-SOURCE-DIAGNOSTICS`

The report records that failure diagnostics use typed/structurally inspectable
records and live responsible-layer vocabulary rather than display strings,
substring checks, or unlayered `Err(String)`-style authority. The evidence covers
pre-proposal context/candidate/planning/source-data diagnostics, post-proposal
event/proposal/projection/checksum/replay diagnostics, content-validation
diagnostics, replay-divergence diagnostics, hidden-truth/source diagnostics, and
debug-only versus actor-visible separation.

Verification:

1. `cargo test -p tracewake-content --test forbidden_content` - passed 20
   tests.
2. `cargo test -p tracewake-core --test event_schema_replay_gates` - passed 17
   tests.
3. `cargo test -p tracewake-core --test no_human_capstone` - passed 1 test.
4. `cargo test -p tracewake-core --test hidden_truth_gates` - passed 13 tests.
5. `cargo test -p tracewake-content --test golden_fixtures_run` - passed 40
   tests, including the named routine/fallback/work-failure diagnostic fixtures.

No engine code changed. The full mutation baseline remains pending from
`0036P0CERPOS0008-001` and is not counted as P0-09 pass evidence.
