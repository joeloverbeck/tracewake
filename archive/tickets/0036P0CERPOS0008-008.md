# 0036P0CERPOS0008-008: P0-07 evidence — debug surfaces are non-diegetic and cannot feed embodied affordances, actor decisions, records, or masquerading test oracles

**Status**: COMPLETED
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-07 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 7 (spec §7.7): debug surfaces must be non-diegetic — no debug-derived fact may become actor cognition, an embodied affordance, institution/record state, validator input, scheduler input, content-selection input, or a masqueraded actor-knowledge test oracle. `EMERGE-OBS` must remain non-gating. This ticket builds the carrier census and gathers the debug-quarantine negatives, then writes the P0-07 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-core/src/{debug_capability,debug_reports}.rs`, `tracewake-tui/src/{debug_panels,render,app}.rs`, `view_models.rs`, `epistemics/projection.rs`, `agent/{actor_known,transaction}.rs`.
2. Spec §7.7 names the corpus; `debug_attach_001`, `view_filtering_001`, `view_model_local_actions_001`, `debug_omniscience_excluded_001`, `embodied_view_omits_raw_assignment_without_context_001`, `embodied_view_omits_unobserved_food_at_open_place_001` exist, and `embodied_view_omits_unknown_sleep_affordance_001` (hedged "if exercised") is present at HEAD (confirmed this session).
3. Shared boundary under audit: the structural separation between embodied view, possession preflight/action menu, transcript, debug panel, and observer-only evidence. The census records, per representative fixture, every actor-visible datum with source event/projection/context IDs and holder-known provenance, and every debug-only row with replay ancestry labeled observer-only.
4. `INV-068` (debug mode is visibly non-diegetic), `INV-107` (debug omniscience is quarantined), and `INV-031` (human/debug notes are non-diegetic) motivate this ticket; `EMERGE-OBS` rows are observer-only per `INV-111`.
5. This ticket audits the debug-quarantine and actor-knowledge surfaces as an evidence consumer. It runs the debug/embodied-view fixtures, the TUI adversarial suite, and `anti_regression_guards.rs` (debug-to-actor carrier guard), recording that no actor-known context cites debug panels/capability/reports/hidden-truth comparison tables/`EMERGE-OBS` rows as modeled knowledge. It weakens no enforcement; a mutation that reads debug reports into view-model generation, actor transaction, action-menu construction, a test oracle posing as actor knowledge, or content validation as modeled fact must be killed (recorded `fail` if surviving).

## Architecture Check

1. A carrier census separating actor-visible / possessed / debug / transcript / observer-only surfaces is stronger than asserting "debug is separate": it proves a test does not pass because a debug panel displayed a fact (spec §11.1 #3).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-107` debug omniscience quarantined -> codebase grep-proof: `debug_omniscience_excluded_001` and the embodied-view omission negatives fail closed.
2. `INV-068`/`INV-031` non-diegetic debug -> manual review: carrier census shows each actor-visible datum has holder-known provenance; debug rows are observer-only with replay ancestry.
3. `EMERGE-OBS` non-gating -> invariants alignment check (`INV-111`): emergence rows inform observer review only, feed no simulation input.

## What to Change

### 1. Build the carrier census

For representative fixtures, record a census of actor-visible surfaces and debug-only surfaces. Each actor-visible datum carries source event/projection/context IDs and holder-known provenance; each debug-only truth row has replay ancestry but is labeled observer-only.

### 2. Record quarantine structure and negatives

Demonstrate structural separation between embodied view, possession preflight/action menu, transcript, debug panel, and observer-only evidence, and that no actor-known context cites debug surfaces or `EMERGE-OBS` rows as modeled knowledge. Map failures to typed diagnostics from {`debug_quarantine`, `view_model`, `holder_known_context`, `tui_input_binding`, `test_oracle`}, distinguishing actor-visible output from debug-only output.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Possession non-reset (P0-04, owned by `-005`) beyond the debug-quarantine carrier boundary.
- The `EMERGE-OBS` staged-abstraction declaration (capstone `-012`); this ticket only confirms it stays non-diegetic.
- Remediation of any debug leakage found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `debug_attach_001` — debug attaches as a quarantined capability.
2. `view_filtering_001`, `view_model_local_actions_001` — embodied view-model filtering holds.
3. Negatives fail closed: `debug_omniscience_excluded_001`, `embodied_view_omits_raw_assignment_without_context_001`, `embodied_view_omits_unobserved_food_at_open_place_001`, `embodied_view_omits_unknown_sleep_affordance_001`.
4. `cargo test -p tracewake-tui --test adversarial_gates` and `cargo test -p tracewake-core --test anti_regression_guards` — debug-to-actor carrier guards hold.

### Invariants

1. No debug-derived fact becomes actor cognition, embodied affordance, institution/record state, validator/scheduler/content-selection input, or a masquerading test oracle (`INV-107`).
2. `EMERGE-OBS` rows remain observer-only and feed no simulation input (`INV-111`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing debug/embodied-view fixtures and TUI adversarial suite; the captured carrier census is the deliverable.`

### Commands

1. `cargo test -p tracewake-tui --test adversarial_gates`
2. `cargo test -p tracewake-core --test anti_regression_guards`
3. `cargo test -p tracewake-content --test golden_fixtures_run` (exercises the debug-attach and embodied-view-omission fixtures)

## Outcome

Completed: 2026-06-16

P0-07 is recorded as `pass` in
`archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md`
with evidence IDs:

1. `0036-P0-07-CARRIER-CENSUS`
2. `0036-P0-07-DEBUG-QUARANTINE-NEGATIVES`
3. `0036-P0-07-OBSERVER-ONLY-ROWS`

The report now records the carrier census for actor-visible, debug-only, TUI
transcript, no-human/debug, and observer-only rows. Actor-visible rows remain
holder-known/source-context backed; debug rows, hidden-truth comparisons,
no-human metrics, and `EMERGE-OBS` rows are labeled observer-only and are not
counted as actor cognition, embodied affordances, validator/scheduler/content
selection inputs, records, institutional state, or actor-knowledge test oracles.

Verification:

1. `cargo test -p tracewake-tui --test adversarial_gates` - passed 15 tests.
2. `cargo test -p tracewake-core --test anti_regression_guards` - passed 80
   tests.
3. `cargo test -p tracewake-content --test golden_fixtures_run` - passed 40
   tests.

No engine code changed. The full mutation baseline remains pending from
`0036P0CERPOS0008-001`; the interrupted local mutant miss is still untriaged and
is not counted as P0-07 pass evidence.
