interface:
  display_name: "Ticket Series"
  short_description: "Implement ticket series with archives."
  default_prompt: "$ticket-series Implement the ticket series TICKETS_GLOB in dependency order. Rely on SPEC_GLOB for reference when provided, including per-ticket archives, final spec or ticket-only closeout, resumed-goal audit, and goal completion."
