--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownDoorFact::canonical_key -> String with "xyzzy".into()
@@ -475,26 +475,17 @@
         self.blocks_movement_when_closed
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
-        format!(
-            "{}:{}->{}:open={}:locked={}:blocks={}:{}",
-            self.door_id.as_str(),
-            self.endpoint_a.as_str(),
-            self.endpoint_b.as_str(),
-            self.is_open,
-            self.is_locked,
-            self.blocks_movement_when_closed,
-            self.source_key
-        )
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub struct ActorKnownContainerFact {
     container_id: ContainerId,
     is_open: bool,
     is_locked: bool,
