--- crates/tracewake-content/src/validate.rs
+++ replace match guard !containers.contains(container_id) with true in validate_location_reference
@@ -869,17 +869,17 @@
     match location {
         Location::AtPlace(place_id) if !places.contains(place_id) => missing(
             errors,
             ValidationPhase::Referential,
             path,
             place_id.as_str(),
             "place",
         ),
-        Location::InContainer(container_id) if !containers.contains(container_id) => missing(
+        Location::InContainer(container_id) if true /* ~ changed by cargo-mutants ~ */ => missing(
             errors,
             ValidationPhase::Referential,
             path,
             container_id.as_str(),
             "container",
         ),
         Location::CarriedBy(actor_id) if !actors.contains(actor_id) => missing(
             errors,
