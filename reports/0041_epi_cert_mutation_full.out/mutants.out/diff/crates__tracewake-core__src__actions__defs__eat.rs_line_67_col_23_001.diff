--- crates/tracewake-core/src/actions/defs/eat.rs
+++ replace - with / in build_eat_events
@@ -59,17 +59,17 @@
         )]);
     }
 
     let consumed = food_consumed_event(
         proposal,
         ordering_key,
         content_manifest_id,
         food,
-        food.servings - 1,
+        food.servings / /* ~ changed by cargo-mutants ~ */ 1,
     );
     let mut events = vec![consumed];
     events.extend(eat_need_events(
         agent_state,
         proposal,
         ordering_key,
         content_manifest_id,
         &events[0],
