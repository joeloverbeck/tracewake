--- crates/tracewake-content/src/manifest.rs
+++ replace ^= with &= in stable_fingerprint
@@ -49,17 +49,17 @@
         payload.extend_from_slice(b"\n");
     }
     payload
 }
 
 pub fn stable_fingerprint(bytes: &[u8]) -> String {
     let mut hash = 0xcbf2_9ce4_8422_2325_u64;
     for byte in bytes {
-        hash ^= u64::from(*byte);
+        hash &= /* ~ changed by cargo-mutants ~ */ u64::from(*byte);
         hash = hash.wrapping_mul(0x0000_0100_0000_01b3);
     }
     format!("twf1-{hash:016x}")
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
