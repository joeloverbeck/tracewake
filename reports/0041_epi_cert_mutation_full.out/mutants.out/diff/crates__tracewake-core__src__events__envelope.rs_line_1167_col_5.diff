--- crates/tracewake-core/src/events/envelope.rs
+++ replace encode_vec -> String with String::new()
@@ -1159,21 +1159,17 @@
     if value.is_empty() {
         Ok(None)
     } else {
         Ok(Some(decode(value)?))
     }
 }
 
 fn encode_vec(values: &[String]) -> String {
-    values
-        .iter()
-        .map(|value| encode(value))
-        .collect::<Vec<_>>()
-        .join(";")
+    String::new() /* ~ changed by cargo-mutants ~ */
 }
 
 fn decode_vec(value: &str) -> Result<Vec<String>, EventEnvelopeParseError> {
     if value.is_empty() {
         Ok(Vec::new())
     } else {
         value.split(';').map(decode).collect()
     }
