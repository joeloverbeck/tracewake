--- crates/tracewake-core/src/agent/generation.rs
+++ replace actor_known_input_refs -> Vec<ActorKnownInputRef> with vec![Default::default()]
@@ -198,20 +198,17 @@
 fn actor_known_fact_notes(actor_known_facts: &[ActorKnownFact]) -> Vec<String> {
     actor_known_facts
         .iter()
         .map(ActorKnownFact::proof_note)
         .collect()
 }
 
 fn actor_known_input_refs(actor_known_facts: &[ActorKnownFact]) -> Vec<ActorKnownInputRef> {
-    actor_known_facts
-        .iter()
-        .map(ActorKnownInputRef::from_fact)
-        .collect()
+    vec![Default::default()] /* ~ changed by cargo-mutants ~ */
 }
 
 #[allow(clippy::too_many_arguments)]
 fn candidate(
     input: &CandidateGenerationInput,
     suffix: &str,
     source: CandidateGoalSource,
     goal_kind: GoalKind,
