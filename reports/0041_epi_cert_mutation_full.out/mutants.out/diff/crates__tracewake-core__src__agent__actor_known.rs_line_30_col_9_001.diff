--- crates/tracewake-core/src/agent/actor_known.rs
+++ replace SourceEventIds::as_slice -> &[EventId] with Vec::leak(vec![Default::default()])
@@ -22,17 +22,17 @@
         if ids.is_empty() {
             Err(SourceEventIdsError::Empty)
         } else {
             Ok(Self { ids })
         }
     }
 
     pub fn as_slice(&self) -> &[EventId] {
-        &self.ids
+        Vec::leak(vec![Default::default()]) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Copy, Debug, PartialEq, Eq)]
 pub enum SourceEventIdsError {
     Empty,
 }
 
