--- crates/tracewake-core/src/state.rs
+++ replace ContainerState::fixed_at_place -> Self with Default::default()
@@ -431,25 +431,17 @@
     pub is_locked: bool,
     pub contents: BTreeSet<ItemId>,
     pub capacity: Option<u32>,
     pub contents_visible_when_closed: bool,
 }
 
 impl ContainerState {
     pub fn fixed_at_place(container_id: ContainerId, place_id: PlaceId) -> Self {
-        Self {
-            container_id,
-            location: Location::AtPlace(place_id),
-            is_open: false,
-            is_locked: false,
-            contents: BTreeSet::new(),
-            capacity: None,
-            contents_visible_when_closed: false,
-        }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq)]
 pub struct ItemState {
     pub item_id: ItemId,
     pub portable: bool,
     pub carry_cost: u32,
