--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ViewMode::stable_id -> &'static str with "xyzzy"
@@ -14,20 +14,17 @@
 #[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub enum ViewMode {
     Embodied,
     Debug,
 }
 
 impl ViewMode {
     pub const fn stable_id(self) -> &'static str {
-        match self {
-            ViewMode::Embodied => "embodied",
-            ViewMode::Debug => "debug",
-        }
+        "xyzzy" /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub enum AllowedKnowledgeSource {
     OwnDirectObservation,
     OwnSearchOrTouchObservation,
     OwnSoundObservation,
