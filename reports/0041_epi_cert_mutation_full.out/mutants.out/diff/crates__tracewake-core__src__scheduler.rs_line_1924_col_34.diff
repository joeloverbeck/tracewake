--- crates/tracewake-core/src/scheduler.rs
+++ replace == with != in no_human::append_routine_step_events_after_proposal
@@ -1916,17 +1916,17 @@
                     window,
                     proposal,
                     content_manifest_id,
                     ordinary_event,
                 ),
             );
             return;
         }
-        if execution_step_status == crate::agent::RoutineStepStatus::NotStarted {
+        if execution_step_status != /* ~ changed by cargo-mutants ~ */ crate::agent::RoutineStepStatus::NotStarted {
             append_and_apply_agent_event(
                 log,
                 agent_state,
                 build_routine_step_started_event(
                     process_id,
                     &execution_id,
                     actor_id,
                     window,
