--- crates/tracewake-core/src/view_models.rs
+++ replace ActionAvailability::actor_safe_summary -> Option<&str> with Some("")
@@ -293,22 +293,17 @@
     pub fn reason_codes(&self) -> &[ReasonCode] {
         match self {
             Self::Available => &[],
             Self::Disabled { reason_codes, .. } => reason_codes,
         }
     }
 
     pub fn actor_safe_summary(&self) -> Option<&str> {
-        match self {
-            Self::Available => None,
-            Self::Disabled {
-                actor_safe_summary, ..
-            } => Some(actor_safe_summary),
-        }
+        Some("") /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn provenance_refs(&self) -> &[ActionAvailabilityProvenance] {
         match self {
             Self::Available => &[],
             Self::Disabled {
                 provenance_refs, ..
             } => provenance_refs,
