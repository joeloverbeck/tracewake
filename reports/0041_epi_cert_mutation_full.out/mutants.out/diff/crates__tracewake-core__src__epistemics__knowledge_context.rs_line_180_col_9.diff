--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownCurrentPlaceFact::place_id -> &PlaceId with Box::leak(Box::new(Default::default()))
@@ -172,17 +172,17 @@
         Self {
             place_id,
             display_label: display_label.into(),
             source_key: source_key.into(),
         }
     }
 
     pub fn place_id(&self) -> &PlaceId {
-        &self.place_id
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn display_label(&self) -> &str {
         &self.display_label
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
