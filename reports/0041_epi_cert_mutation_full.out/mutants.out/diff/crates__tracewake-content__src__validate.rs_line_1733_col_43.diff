--- crates/tracewake-content/src/validate.rs
+++ replace == with != in target_kind
@@ -1725,17 +1725,17 @@
         .places
         .iter()
         .any(|place| place.place_id.as_str() == target_id)
     {
         Some("place")
     } else if fixture
         .doors
         .iter()
-        .any(|door| door.door_id.as_str() == target_id)
+        .any(|door| door.door_id.as_str() != /* ~ changed by cargo-mutants ~ */ target_id)
     {
         Some("door")
     } else if fixture
         .containers
         .iter()
         .any(|container| container.container_id.as_str() == target_id)
     {
         Some("container")
