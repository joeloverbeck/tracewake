--- crates/tracewake-core/src/events/log.rs
+++ replace EventLog::events -> &[EventEnvelope] with Vec::leak(vec![Default::default()])
@@ -64,17 +64,17 @@
                 actual: event.stream_position,
             });
         }
         self.events.push(event.clone());
         Ok(event)
     }
 
     pub fn events(&self) -> &[EventEnvelope] {
-        &self.events
+        Vec::leak(vec![Default::default()]) /* ~ changed by cargo-mutants ~ */
     }
 
     #[cfg(test)]
     pub(crate) fn from_ordered_events_for_replay_tests(events: Vec<EventEnvelope>) -> Self {
         Self { events }
     }
 
     pub fn serialize_canonical(&self) -> Vec<u8> {
