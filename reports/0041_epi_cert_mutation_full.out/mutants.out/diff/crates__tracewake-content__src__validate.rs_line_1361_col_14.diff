--- crates/tracewake-content/src/validate.rs
+++ replace == with != in validate_duration_u64
@@ -1353,17 +1353,17 @@
     }
 }
 
 fn validate_duration_u64(
     value: u64,
     path: impl Into<String>,
     errors: &mut Vec<ContentValidationError>,
 ) {
-    if value == 0 {
+    if value != /* ~ changed by cargo-mutants ~ */ 0 {
         errors.push(ContentValidationError::new(
             ValidationPhase::State,
             path,
             "invalid_duration",
             "duration values must be greater than zero",
         ));
     }
 }
