--- crates/tracewake-core/src/actions/report.rs
+++ replace CheckedFact::render_pair -> String with String::new()
@@ -202,17 +202,17 @@
         &self.value
     }
 
     pub const fn source(&self) -> CheckedFactSource {
         self.source
     }
 
     pub fn render_pair(&self) -> String {
-        format!("{}={}", self.stable_key(), self.value)
+        String::new() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq)]
 pub struct ValidationReport {
     pub validation_report_id: ValidationReportId,
     pub proposal_id: ProposalId,
     pub actor_id: Option<ActorId>,
