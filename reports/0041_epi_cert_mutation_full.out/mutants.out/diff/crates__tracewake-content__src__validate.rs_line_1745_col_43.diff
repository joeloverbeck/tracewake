--- crates/tracewake-content/src/validate.rs
+++ replace == with != in target_kind
@@ -1737,17 +1737,17 @@
         .containers
         .iter()
         .any(|container| container.container_id.as_str() == target_id)
     {
         Some("container")
     } else if fixture
         .items
         .iter()
-        .any(|item| item.item_id.as_str() == target_id)
+        .any(|item| item.item_id.as_str() != /* ~ changed by cargo-mutants ~ */ target_id)
     {
         Some("item")
     } else if fixture
         .food_supplies
         .iter()
         .any(|food| food.food_supply_id.as_str() == target_id)
     {
         Some("food_supply")
