--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownDoorFact::door_id -> &DoorId with Box::leak(Box::new(Default::default()))
@@ -447,17 +447,17 @@
             is_open,
             is_locked,
             blocks_movement_when_closed,
             source_key: source_key.into(),
         }
     }
 
     pub fn door_id(&self) -> &DoorId {
-        &self.door_id
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn endpoint_a(&self) -> &PlaceId {
         &self.endpoint_a
     }
 
     pub fn endpoint_b(&self) -> &PlaceId {
         &self.endpoint_b
