--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownCurrentPlaceFact::canonical_key -> String with String::new()
@@ -184,22 +184,17 @@
         &self.display_label
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
-        format!(
-            "{}:label={}:{}",
-            self.place_id.as_str(),
-            self.display_label,
-            self.source_key
-        )
+        String::new() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub struct ActorKnownCarriedItemFact {
     item_id: ItemId,
     source: Location,
     portable: bool,
