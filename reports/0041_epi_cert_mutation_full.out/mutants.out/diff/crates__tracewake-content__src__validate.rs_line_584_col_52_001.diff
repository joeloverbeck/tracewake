--- crates/tracewake-content/src/validate.rs
+++ replace match guard !containers.contains(container_id) with false in validate_references
@@ -576,17 +576,17 @@
         match &item.location {
             Location::AtPlace(place_id) if !places.contains(place_id) => missing(
                 errors,
                 ValidationPhase::Referential,
                 format!("items[{index}].location"),
                 place_id.as_str(),
                 "place",
             ),
-            Location::InContainer(container_id) if !containers.contains(container_id) => missing(
+            Location::InContainer(container_id) if false /* ~ changed by cargo-mutants ~ */ => missing(
                 errors,
                 ValidationPhase::Referential,
                 format!("items[{index}].location"),
                 container_id.as_str(),
                 "container",
             ),
             Location::CarriedBy(actor_id) if !actors.contains(actor_id) => missing(
                 errors,
