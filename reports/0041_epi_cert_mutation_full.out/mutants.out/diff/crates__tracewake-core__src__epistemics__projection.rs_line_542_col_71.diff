--- crates/tracewake-core/src/epistemics/projection.rs
+++ replace == with != in EpistemicProjection::classified_actor_known_records_for_context
@@ -534,17 +534,17 @@
             .into_iter()
             .flat_map(|records| records.iter())
             .filter(|record| {
                 record
                     .policy()
                     .includes_classified_record(record, &selected_workplaces)
             })
             .map(|record| {
-                let current_place_record = record.relevant_place_id() == current_place_id;
+                let current_place_record = record.relevant_place_id() != /* ~ changed by cargo-mutants ~ */ current_place_id;
                 let latest_current_place_record = is_latest_current_place_record(
                     record,
                     current_place_id,
                     latest_current_place_tick,
                 );
                 ClassifiedActorKnownProjectionRecord {
                     record,
                     freshness: record.policy().freshness(
