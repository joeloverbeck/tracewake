--- crates/tracewake-core/src/events/apply.rs
+++ delete ! in assert_single_tick_delta_charge
@@ -1232,17 +1232,17 @@
         .sim_tick
         .value()
         .saturating_sub(elapsed_ticks)
         .saturating_add(1);
     for tick in first_tick..=event.sim_tick.value() {
         let inserted = state
             .need_tick_charges
             .insert((actor_id.clone(), need_kind, tick));
-        if !inserted {
+        if  /* ~ changed by cargo-mutants ~ */inserted {
             return Err(ApplyError::DuplicateNeedTickCharge {
                 actor_id: actor_id.clone(),
                 need_kind,
                 tick,
             });
         }
     }
     Ok(())
