--- crates/tracewake-core/src/view_models.rs
+++ replace ActionAvailability::provenance_refs -> &[ActionAvailabilityProvenance] with Vec::leak(Vec::new())
@@ -302,22 +302,17 @@
             Self::Available => None,
             Self::Disabled {
                 actor_safe_summary, ..
             } => Some(actor_safe_summary),
         }
     }
 
     pub fn provenance_refs(&self) -> &[ActionAvailabilityProvenance] {
-        match self {
-            Self::Available => &[],
-            Self::Disabled {
-                provenance_refs, ..
-            } => provenance_refs,
-        }
+        Vec::leak(Vec::new()) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn debug_only_diagnostics(&self) -> &[String] {
         match self {
             Self::Available => &[],
             Self::Disabled {
                 debug_only_diagnostics,
                 ..
