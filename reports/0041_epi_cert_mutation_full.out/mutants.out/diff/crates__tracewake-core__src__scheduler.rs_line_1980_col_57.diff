--- crates/tracewake-core/src/scheduler.rs
+++ replace && with || in no_human::is_instant_routine_progress_event
@@ -1972,17 +1972,17 @@
         )
     }
 
     fn is_instant_routine_progress_event(
         execution_family: RoutineFamily,
         proposal: &Proposal,
         event: &EventEnvelope,
     ) -> bool {
-        if execution_family == RoutineFamily::WorkBlock && proposal.action_id.as_str() == "move" {
+        if execution_family == RoutineFamily::WorkBlock || /* ~ changed by cargo-mutants ~ */ proposal.action_id.as_str() == "move" {
             return false;
         }
         !matches!(
             event.event_type,
             EventKind::SleepStarted | EventKind::WorkBlockStarted
         )
     }
 
