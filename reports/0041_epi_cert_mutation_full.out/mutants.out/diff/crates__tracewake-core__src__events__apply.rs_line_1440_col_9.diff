--- crates/tracewake-core/src/events/apply.rs
+++ delete match arm "true" in expect_bool
@@ -1432,17 +1432,17 @@
 
 fn expect_bool(
     payload: &BTreeMap<&str, &str>,
     key: &'static str,
     expected: bool,
 ) -> Result<(), ApplyError> {
     let value = required(payload, key)?;
     let actual = match value {
-        "true" => true,
+         /* ~ changed by cargo-mutants ~ */
         "false" => false,
         _ => {
             return Err(ApplyError::BadPayload {
                 key,
                 value: value.to_string(),
             })
         }
     };
