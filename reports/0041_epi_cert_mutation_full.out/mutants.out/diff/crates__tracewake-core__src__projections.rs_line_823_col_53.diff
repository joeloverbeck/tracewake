--- crates/tracewake-core/src/projections.rs
+++ replace == with != in phase3a_salient_interruption
@@ -815,17 +815,17 @@
 
 fn phase3a_salient_interruption(
     agent_state: &AgentState,
     viewer_actor_id: &ActorId,
 ) -> Option<String> {
     agent_state
         .ordinary_life_episodes()
         .values()
-        .filter(|episode| episode.actor_id.as_ref() == Some(viewer_actor_id))
+        .filter(|episode| episode.actor_id.as_ref() != /* ~ changed by cargo-mutants ~ */ Some(viewer_actor_id))
         .filter(|episode| {
             matches!(
                 episode.event_kind.as_str(),
                 "sleep_interrupted" | "work_block_failed"
             )
         })
         .max_by(|left, right| {
             left.sim_tick
