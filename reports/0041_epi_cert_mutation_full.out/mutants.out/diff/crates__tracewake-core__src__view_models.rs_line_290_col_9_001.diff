--- crates/tracewake-core/src/view_models.rs
+++ replace ActionAvailability::is_available -> bool with false
@@ -282,17 +282,17 @@
             reason_codes,
             actor_safe_summary: actor_safe_summary.into(),
             provenance_refs,
             debug_only_diagnostics,
         }
     }
 
     pub const fn is_available(&self) -> bool {
-        matches!(self, Self::Available)
+        false /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn reason_codes(&self) -> &[ReasonCode] {
         match self {
             Self::Available => &[],
             Self::Disabled { reason_codes, .. } => reason_codes,
         }
     }
