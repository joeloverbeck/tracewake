--- crates/tracewake-core/src/state.rs
+++ replace PhysicalState::actors -> &BTreeMap<ActorId, ActorBody> with Box::leak(Box::new(BTreeMap::new()))
@@ -240,17 +240,17 @@
             food_supplies,
             workplaces,
             sleep_affordances,
             need_model,
         }
     }
 
     pub fn actors(&self) -> &BTreeMap<ActorId, ActorBody> {
-        &self.actors
+        Box::leak(Box::new(BTreeMap::new())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn places(&self) -> &BTreeMap<PlaceId, PlaceState> {
         &self.places
     }
 
     pub fn doors(&self) -> &BTreeMap<DoorId, DoorState> {
         &self.doors
