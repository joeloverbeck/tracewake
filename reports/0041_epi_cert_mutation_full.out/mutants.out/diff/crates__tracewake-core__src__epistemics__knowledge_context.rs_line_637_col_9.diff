--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownRouteFact::from_place_id -> &PlaceId with Box::leak(Box::new(Default::default()))
@@ -629,17 +629,17 @@
         Self {
             from_place_id,
             to_place_id,
             source_key: source_key.into(),
         }
     }
 
     pub fn from_place_id(&self) -> &PlaceId {
-        &self.from_place_id
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn to_place_id(&self) -> &PlaceId {
         &self.to_place_id
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
