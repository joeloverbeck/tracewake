--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace KnowledgeProvenanceEntry::canonical_key -> String with "xyzzy".into()
@@ -142,22 +142,17 @@
         self.source
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
-        format!(
-            "{}:{}:{}",
-            self.kind.stable_id(),
-            self.source.stable_id(),
-            self.source_key
-        )
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub struct ActorKnownCurrentPlaceFact {
     place_id: PlaceId,
     display_label: String,
     source_key: String,
