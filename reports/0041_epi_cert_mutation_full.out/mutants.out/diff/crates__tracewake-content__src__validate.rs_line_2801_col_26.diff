--- crates/tracewake-content/src/validate.rs
+++ replace || with && in is_forbidden_key
@@ -2793,17 +2793,17 @@
             "bad_confidence",
             format!("{error:?}"),
         ),
     };
     ContentValidationError::new(ValidationPhase::ParseSchema, path, code, message)
 }
 
 fn is_forbidden_key(value: &str) -> bool {
-    is_player_key(value) || is_script_key(value)
+    is_player_key(value) && /* ~ changed by cargo-mutants ~ */ is_script_key(value)
 }
 
 fn is_player_key(value: &str) -> bool {
     matches!(
         value,
         "player"
             | "player_character"
             | "player_only"
