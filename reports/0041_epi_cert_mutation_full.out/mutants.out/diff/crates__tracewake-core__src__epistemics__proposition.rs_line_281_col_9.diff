--- crates/tracewake-core/src/epistemics/proposition.rs
+++ replace <impl fmt::Display for Proposition>::fmt -> fmt::Result with Ok(Default::default())
@@ -273,17 +273,17 @@
             ) => item_id == missing_item_id && actor_id == expected_actor_id,
             _ => false,
         }
     }
 }
 
 impl fmt::Display for Proposition {
     fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
-        f.write_str(&self.render())
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl FromStr for Proposition {
     type Err = PropositionParseError;
 
     fn from_str(value: &str) -> Result<Self, Self::Err> {
         let parts: Vec<_> = value.split('|').collect();
