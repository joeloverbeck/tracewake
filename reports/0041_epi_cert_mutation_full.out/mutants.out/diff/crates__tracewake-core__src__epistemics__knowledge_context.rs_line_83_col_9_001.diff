--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ScopeFilter::canonical_key -> String with "xyzzy".into()
@@ -75,22 +75,17 @@
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub enum ScopeFilter {
     ActorPrivate(ActorId),
     DebugAll,
 }
 
 impl ScopeFilter {
     fn canonical_key(&self) -> String {
-        match self {
-            ScopeFilter::ActorPrivate(actor_id) => {
-                format!("actor_private:{}", actor_id.as_str())
-            }
-            ScopeFilter::DebugAll => "debug_all".to_string(),
-        }
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub enum KnowledgeProvenanceKind {
     Perception,
     Memory,
     Observation,
