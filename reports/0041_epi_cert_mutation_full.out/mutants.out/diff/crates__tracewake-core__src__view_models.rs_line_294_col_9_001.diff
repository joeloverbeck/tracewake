--- crates/tracewake-core/src/view_models.rs
+++ replace ActionAvailability::reason_codes -> &[ReasonCode] with Vec::leak(vec![Default::default()])
@@ -286,20 +286,17 @@
         }
     }
 
     pub const fn is_available(&self) -> bool {
         matches!(self, Self::Available)
     }
 
     pub fn reason_codes(&self) -> &[ReasonCode] {
-        match self {
-            Self::Available => &[],
-            Self::Disabled { reason_codes, .. } => reason_codes,
-        }
+        Vec::leak(vec![Default::default()]) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn actor_safe_summary(&self) -> Option<&str> {
         match self {
             Self::Available => None,
             Self::Disabled {
                 actor_safe_summary, ..
             } => Some(actor_safe_summary),
