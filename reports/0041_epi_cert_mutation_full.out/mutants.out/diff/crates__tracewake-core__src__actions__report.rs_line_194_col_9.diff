--- crates/tracewake-core/src/actions/report.rs
+++ replace CheckedFact::key -> &CheckedFactKey with Box::leak(Box::new(Default::default()))
@@ -186,17 +186,17 @@
         Self {
             key: CheckedFactKey::from_stable_key(key),
             value: value.into(),
             source: CheckedFactSource::Validation,
         }
     }
 
     pub const fn key(&self) -> &CheckedFactKey {
-        &self.key
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn stable_key(&self) -> &str {
         self.key.stable_key()
     }
 
     pub fn value(&self) -> &str {
         &self.value
