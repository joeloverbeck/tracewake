--- crates/tracewake-core/src/actions/defs/work.rs
+++ replace > with >= in build_work_start_events
@@ -72,17 +72,17 @@
             proposal,
             ordering_key,
             content_manifest_id,
             workplace,
             "need",
             "fatigue state missing for work",
         )]);
     };
-    if i32::from(current_fatigue) > workplace.max_fatigue_to_start {
+    if i32::from(current_fatigue) >= /* ~ changed by cargo-mutants ~ */ workplace.max_fatigue_to_start {
         return Ok(vec![work_failed_event(
             proposal,
             ordering_key,
             content_manifest_id,
             workplace,
             "need",
             "fatigue too high for work",
         )]);
