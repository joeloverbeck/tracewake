--- crates/tracewake-core/src/scheduler.rs
+++ replace no_human::active_intention_for_actor -> Option<Intention> with Some(Default::default())
@@ -879,18 +879,17 @@
                     .then_with(|| left.execution_id.cmp(&right.execution_id))
             })
     }
 
     fn active_intention_for_actor(
         agent_state: &AgentState,
         actor_id: &ActorId,
     ) -> Option<Intention> {
-        let intention_id = agent_state.active_intention_by_actor.get(actor_id)?;
-        agent_state.intentions.get(intention_id).cloned()
+        Some(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 
     enum PendingCompletion {
         Sleep(EventEnvelope),
         Work(EventEnvelope),
     }
 
     impl PendingCompletion {
