--- crates/tracewake-core/src/agent/actor_known.rs
+++ replace derive_hidden_truth_audit -> HiddenTruthAudit with Default::default()
@@ -528,17 +528,17 @@
         derived
     }
 }
 
 pub fn derive_hidden_truth_audit(
     context: &ActorKnownPlanningContext,
     request_facts: &[ActorKnownFact],
 ) -> HiddenTruthAudit {
-    context.audit_with(request_facts)
+    Default::default() /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
 
     fn actor_id() -> ActorId {
         ActorId::new("actor_tomas").unwrap()
