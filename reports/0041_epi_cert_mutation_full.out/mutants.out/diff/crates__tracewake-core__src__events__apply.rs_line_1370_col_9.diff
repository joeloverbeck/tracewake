--- crates/tracewake-core/src/events/apply.rs
+++ delete match arm EventKind::IntentionInterrupted in apply_intention_transition
@@ -1362,24 +1362,17 @@
         EventKind::IntentionAbandoned => {
             intention
                 .abandon(reason)
                 .map_err(|err| ApplyError::BadPayload {
                     key: "status",
                     value: format!("{err:?}"),
                 })?
         }
-        EventKind::IntentionInterrupted => {
-            intention
-                .interrupt(reason)
-                .map_err(|err| ApplyError::BadPayload {
-                    key: "status",
-                    value: format!("{err:?}"),
-                })?
-        }
+         /* ~ changed by cargo-mutants ~ */
         _ => {}
     }
     Ok(())
 }
 
 fn apply_routine_step_transition(
     state: &mut AgentState,
     event_type: EventKind,
