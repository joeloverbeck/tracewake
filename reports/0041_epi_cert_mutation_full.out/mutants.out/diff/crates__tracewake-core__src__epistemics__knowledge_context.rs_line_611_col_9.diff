--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownLocalActorFact::actor_id -> &ActorId with Box::leak(Box::new(Default::default()))
@@ -603,17 +603,17 @@
     pub fn new(actor_id: ActorId, source_key: impl Into<String>) -> Self {
         Self {
             actor_id,
             source_key: source_key.into(),
         }
     }
 
     pub fn actor_id(&self) -> &ActorId {
-        &self.actor_id
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
         format!("{}:{}", self.actor_id.as_str(), self.source_key)
