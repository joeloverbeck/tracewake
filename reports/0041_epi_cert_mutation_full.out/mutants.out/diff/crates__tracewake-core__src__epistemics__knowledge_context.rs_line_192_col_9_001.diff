--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownCurrentPlaceFact::canonical_key -> String with "xyzzy".into()
@@ -184,22 +184,17 @@
         &self.display_label
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
-        format!(
-            "{}:label={}:{}",
-            self.place_id.as_str(),
-            self.display_label,
-            self.source_key
-        )
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub struct ActorKnownCarriedItemFact {
     item_id: ItemId,
     source: Location,
     portable: bool,
