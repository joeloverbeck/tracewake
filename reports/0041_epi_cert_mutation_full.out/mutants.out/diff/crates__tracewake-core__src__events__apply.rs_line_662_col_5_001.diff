--- crates/tracewake-core/src/events/apply.rs
+++ replace required -> Result<&'a str, ApplyError> with Ok("xyzzy")
@@ -654,20 +654,17 @@
         .map(|field| (field.key.as_str(), field.value.as_str()))
         .collect()
 }
 
 fn required<'a>(
     payload: &'a BTreeMap<&str, &str>,
     key: &'static str,
 ) -> Result<&'a str, ApplyError> {
-    payload
-        .get(key)
-        .copied()
-        .ok_or(ApplyError::MissingPayload(key))
+    Ok("xyzzy") /* ~ changed by cargo-mutants ~ */
 }
 
 fn require_payload_version(
     payload: &BTreeMap<&str, &str>,
     key: &'static str,
     expected: &'static str,
 ) -> Result<(), ApplyError> {
     let actual = required(payload, key)?;
