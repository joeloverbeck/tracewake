--- crates/tracewake-core/src/projections.rs
+++ replace proposal_from_current_view_semantic_action -> Proposal with Default::default()
@@ -1357,25 +1357,17 @@
 pub fn proposal_from_current_view_semantic_action(
     proposal_id: ProposalId,
     actor_id: ActorId,
     requested_tick: SimTick,
     entry: &SemanticActionEntry,
     source_view: &EmbodiedViewModel,
     controller_id: &ControllerId,
 ) -> Proposal {
-    proposal_from_semantic_action_entry(
-        proposal_id,
-        ProposalOrigin::Human,
-        Some(actor_id),
-        requested_tick,
-        entry,
-        Some(source_view),
-        Some(controller_id),
-    )
+    Default::default() /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::agent::{
         Intention, IntentionSource, NeedChangeCause, NeedKind, NeedState, SourceEventIds,
     };
