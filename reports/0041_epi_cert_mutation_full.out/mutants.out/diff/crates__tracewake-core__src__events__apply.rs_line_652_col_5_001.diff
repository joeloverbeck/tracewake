--- crates/tracewake-core/src/events/apply.rs
+++ replace payload_map -> BTreeMap<&str, &str> with BTreeMap::from_iter([("", "")])
@@ -644,20 +644,17 @@
                 });
             }
         }
     }
     Ok(())
 }
 
 fn payload_map(payload: &[PayloadField]) -> BTreeMap<&str, &str> {
-    payload
-        .iter()
-        .map(|field| (field.key.as_str(), field.value.as_str()))
-        .collect()
+    BTreeMap::from_iter([("", "")]) /* ~ changed by cargo-mutants ~ */
 }
 
 fn required<'a>(
     payload: &'a BTreeMap<&str, &str>,
     key: &'static str,
 ) -> Result<&'a str, ApplyError> {
     payload
         .get(key)
