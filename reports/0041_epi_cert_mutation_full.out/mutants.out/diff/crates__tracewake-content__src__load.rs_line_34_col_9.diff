--- crates/tracewake-content/src/load.rs
+++ replace <impl From<ContentValidationFailure> for LoadError>::from -> Self with Default::default()
@@ -26,17 +26,17 @@
 impl From<SerializationError> for LoadError {
     fn from(value: SerializationError) -> Self {
         Self::Serialization(value)
     }
 }
 
 impl From<ContentValidationFailure> for LoadError {
     fn from(value: ContentValidationFailure) -> Self {
-        Self::Validation(value)
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq)]
 pub struct SourceFile {
     pub path: String,
     pub bytes: Vec<u8>,
 }
