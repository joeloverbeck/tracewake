--- crates/tracewake-core/src/agent/no_human_surface.rs
+++ replace SealedActorKnownSurface::context -> &ActorKnownPlanningContext with Box::leak(Box::new(Default::default()))
@@ -15,17 +15,17 @@
 }
 
 impl SealedActorKnownSurface {
     fn new(context: ActorKnownPlanningContext) -> Self {
         Self { context }
     }
 
     pub fn context(&self) -> &ActorKnownPlanningContext {
-        &self.context
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn into_context(self) -> ActorKnownPlanningContext {
         self.context
     }
 }
 
 /// Event-log-backed actor-known surface builder for no-human decisions.
