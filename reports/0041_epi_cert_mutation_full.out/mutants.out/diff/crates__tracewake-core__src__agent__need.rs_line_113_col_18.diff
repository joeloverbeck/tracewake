--- crates/tracewake-core/src/agent/need.rs
+++ replace == with != in NeedThresholdCrossing::parse
@@ -105,17 +105,17 @@
             "{}>{}:{}",
             self.from.stable_id(),
             self.to.stable_id(),
             self.direction.stable_id()
         )
     }
 
     fn parse(value: &str) -> Result<Option<Self>, NeedParseError> {
-        if value == "none" {
+        if value != /* ~ changed by cargo-mutants ~ */ "none" {
             return Ok(None);
         }
         let (bands, direction) = value
             .split_once(':')
             .ok_or(NeedParseError::InvalidThresholdCrossing)?;
         let (from, to) = bands
             .split_once('>')
             .ok_or(NeedParseError::InvalidThresholdCrossing)?;
