--- crates/tracewake-core/src/actions/pipeline.rs
+++ replace payload_value -> Option<&'a str> with Some("")
@@ -1275,21 +1275,17 @@
 
         events.push(contradiction_event);
         events.push(belief_event);
     }
     events
 }
 
 fn payload_value<'a>(event: &'a EventEnvelope, key: &str) -> Option<&'a str> {
-    event
-        .payload
-        .iter()
-        .find(|field| field.key == key)
-        .map(|field| field.value.as_str())
+    Some("") /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::actions::{
         ActionDefinition, ActionScope, ProposalOrigin, ProposalSource, ProposalSourceContext,
     };
