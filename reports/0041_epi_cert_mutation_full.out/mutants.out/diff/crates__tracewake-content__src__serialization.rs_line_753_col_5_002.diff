--- crates/tracewake-content/src/serialization.rs
+++ replace split_encoded -> Result<Vec<String>, SerializationError> with Ok(vec!["xyzzy".into()])
@@ -745,21 +745,17 @@
     values
         .iter()
         .map(|value| encode(value))
         .collect::<Vec<_>>()
         .join(",")
 }
 
 fn split_encoded(value: &str) -> Result<Vec<String>, SerializationError> {
-    if value.is_empty() {
-        Ok(Vec::new())
-    } else {
-        value.split(',').map(decode).collect()
-    }
+    Ok(vec!["xyzzy".into()]) /* ~ changed by cargo-mutants ~ */
 }
 
 fn join_usize(values: &[usize]) -> String {
     values
         .iter()
         .map(usize::to_string)
         .collect::<Vec<_>>()
         .join(",")
