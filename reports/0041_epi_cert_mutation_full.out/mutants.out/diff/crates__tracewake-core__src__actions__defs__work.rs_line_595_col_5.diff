--- crates/tracewake-core/src/actions/defs/work.rs
+++ replace reject -> ActionRejection with Default::default()
@@ -587,23 +587,17 @@
 fn reject(
     failed_stage: PipelineStage,
     reason_code: ReasonCode,
     key: &'static str,
     value: &str,
     actor_visible_summary: impl Into<String>,
     debug_summary: impl Into<String>,
 ) -> ActionRejection {
-    ActionRejection::new(
-        failed_stage,
-        reason_code,
-        vec![CheckedFact::new(key, value)],
-        actor_visible_summary,
-        debug_summary,
-    )
+    Default::default() /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::actions::pipeline::{run_pipeline, PipelineContext};
     use crate::actions::proposal::ProposalOrigin;
     use crate::actions::registry::ActionRegistry;
