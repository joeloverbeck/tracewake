--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownRouteFact::canonical_key -> String with String::new()
@@ -641,22 +641,17 @@
         &self.to_place_id
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
-        format!(
-            "{}->{}:{}",
-            self.from_place_id.as_str(),
-            self.to_place_id.as_str(),
-            self.source_key
-        )
+        String::new() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq)]
 pub struct ForbiddenTruthAudit {
     excluded_sources: BTreeSet<ForbiddenKnowledgeSource>,
     passed: bool,
 }
