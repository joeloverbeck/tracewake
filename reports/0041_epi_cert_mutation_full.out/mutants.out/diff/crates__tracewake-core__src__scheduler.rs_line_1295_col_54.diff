--- crates/tracewake-core/src/scheduler.rs
+++ replace && with || in no_human::routine_stuck_diagnostic_kinds
@@ -1287,17 +1287,17 @@
             .routine_executions()
             .iter()
             .filter(|(_, execution)| &execution.actor_id == actor_id)
             .flat_map(|(execution_id, execution)| {
                 let mut diagnostics = Vec::new();
                 if execution
                     .expected_next_progress_tick
                     .is_some_and(|expected| {
-                        expected < window.start_tick && execution.last_progress_tick < expected
+                        expected < window.start_tick || /* ~ changed by cargo-mutants ~ */ execution.last_progress_tick < expected
                     })
                 {
                     diagnostics.push((
                         StuckDiagnosticKind::PastExpectedProgressWindow,
                         execution_id.clone(),
                     ));
                 }
                 if execution.step_status == RoutineStepStatus::Waiting
