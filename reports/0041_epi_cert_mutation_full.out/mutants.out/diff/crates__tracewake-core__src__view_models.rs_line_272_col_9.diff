--- crates/tracewake-core/src/view_models.rs
+++ replace ActionAvailability::available -> Self with Default::default()
@@ -264,17 +264,17 @@
         actor_safe_summary: String,
         provenance_refs: Vec<ActionAvailabilityProvenance>,
         debug_only_diagnostics: Vec<String>,
     },
 }
 
 impl ActionAvailability {
     pub const fn available() -> Self {
-        Self::Available
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn disabled(
         reason_codes: Vec<ReasonCode>,
         actor_safe_summary: impl Into<String>,
         provenance_refs: Vec<ActionAvailabilityProvenance>,
         debug_only_diagnostics: Vec<String>,
     ) -> Self {
