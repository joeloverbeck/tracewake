--- crates/tracewake-content/src/validate.rs
+++ replace == with != in target_kind
@@ -1755,17 +1755,17 @@
         .workplaces
         .iter()
         .any(|workplace| workplace.workplace_id.as_str() == target_id)
     {
         Some("workplace")
     } else if fixture
         .routine_templates
         .iter()
-        .any(|template| template.template_id.as_str() == target_id)
+        .any(|template| template.template_id.as_str() != /* ~ changed by cargo-mutants ~ */ target_id)
     {
         Some("routine_template")
     } else {
         None
     }
 }
 
 fn validate_semantic_ids(fixture: &FixtureSchema, errors: &mut Vec<ContentValidationError>) {
