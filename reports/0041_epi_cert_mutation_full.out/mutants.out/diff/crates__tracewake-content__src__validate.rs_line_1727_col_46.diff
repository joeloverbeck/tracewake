--- crates/tracewake-content/src/validate.rs
+++ replace == with != in target_kind
@@ -1719,17 +1719,17 @@
         .actors
         .iter()
         .any(|actor| actor.actor_id.as_str() == target_id)
     {
         Some("actor")
     } else if fixture
         .places
         .iter()
-        .any(|place| place.place_id.as_str() == target_id)
+        .any(|place| place.place_id.as_str() != /* ~ changed by cargo-mutants ~ */ target_id)
     {
         Some("place")
     } else if fixture
         .doors
         .iter()
         .any(|door| door.door_id.as_str() == target_id)
     {
         Some("door")
