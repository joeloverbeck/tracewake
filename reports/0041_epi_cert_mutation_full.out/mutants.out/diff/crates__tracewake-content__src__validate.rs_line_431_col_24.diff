--- crates/tracewake-content/src/validate.rs
+++ replace > with == in validate_ids
@@ -423,17 +423,17 @@
                     edge.actor_id.as_str(),
                     edge.food_supply_id.as_str()
                 ),
             ));
         }
     }
 
     for (id, paths) in seen {
-        if paths.len() > 1 {
+        if paths.len() == /* ~ changed by cargo-mutants ~ */ 1 {
             errors.push(ContentValidationError::new(
                 ValidationPhase::Id,
                 paths.join(","),
                 "duplicate_id",
                 format!("duplicate stable ID {id} at {}", paths.join(",")),
             ));
         }
     }
