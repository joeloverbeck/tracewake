--- crates/tracewake-core/src/actions/defs/work.rs
+++ replace need_value -> Option<u16> with Some(0)
@@ -540,21 +540,17 @@
     })
 }
 
 fn need_value(
     agent_state: &AgentState,
     actor_id: &crate::ids::ActorId,
     need_kind: NeedKind,
 ) -> Option<u16> {
-    agent_state
-        .needs_by_actor
-        .get(actor_id)
-        .and_then(|needs| needs.get(&need_kind))
-        .map(crate::agent::NeedState::value)
+    Some(0) /* ~ changed by cargo-mutants ~ */
 }
 
 fn payload_value<'a>(event: &'a EventEnvelope, key: &'static str) -> Result<&'a str, ApplyError> {
     event
         .payload
         .iter()
         .find(|field| field.key == key)
         .map(|field| field.value.as_str())
