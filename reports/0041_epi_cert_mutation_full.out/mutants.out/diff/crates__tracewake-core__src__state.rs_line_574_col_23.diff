--- crates/tracewake-core/src/state.rs
+++ replace == with != in FoodSupplyState::is_empty
@@ -566,17 +566,17 @@
             food_supply_id,
             location,
             servings,
             hunger_reduction_per_serving,
         }
     }
 
     pub fn is_empty(&self) -> bool {
-        self.servings == 0
+        self.servings != /* ~ changed by cargo-mutants ~ */ 0
     }
 }
 
 impl ItemState {
     pub fn new(item_id: ItemId, location: Location) -> Self {
         Self {
             item_id,
             portable: true,
