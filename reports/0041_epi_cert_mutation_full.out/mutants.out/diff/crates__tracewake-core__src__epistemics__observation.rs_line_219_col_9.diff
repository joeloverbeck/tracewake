--- crates/tracewake-core/src/epistemics/observation.rs
+++ replace Observation::privacy_scope -> &PrivacyScope with Box::leak(Box::new(Default::default()))
@@ -211,17 +211,17 @@
         &self.alternatives
     }
 
     pub fn schema_version(&self) -> &SchemaVersion {
         &self.schema_version
     }
 
     pub fn privacy_scope(&self) -> &PrivacyScope {
-        &self.privacy_scope
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[cfg(test)]
 mod tests {
     use super::*;
     use crate::ids::ObservationId;
 
