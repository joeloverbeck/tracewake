--- crates/tracewake-core/src/agent/candidate.rs
+++ replace CandidateGoal::ordering_key -> (u8, &ActorId, &CandidateGoalId) with (1, Box::leak(Box::new(Default::default())), Box::leak(Box::new(Default::default())))
@@ -151,21 +151,17 @@
             applicability,
             rejection_reason,
             selected_routine_method,
             trace_id,
         }
     }
 
     fn ordering_key(&self) -> (u8, &ActorId, &CandidateGoalId) {
-        (
-            self.priority.selection_rank(),
-            &self.actor_id,
-            &self.candidate_goal_id,
-        )
+        (1, Box::leak(Box::new(Default::default())), Box::leak(Box::new(Default::default()))) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl Ord for CandidateGoal {
     fn cmp(&self, other: &Self) -> Ordering {
         self.ordering_key().cmp(&other.ordering_key())
     }
 }
