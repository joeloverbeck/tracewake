--- crates/tracewake-core/src/controller.rs
+++ replace ControllerBindings::debug_bindings -> Vec<&RuntimeControllerBinding> with vec![Box::leak(Box::new(Default::default()))]
@@ -99,17 +99,17 @@
         log.append(event).expect("controller event is versioned")
     }
 
     pub fn binding(&self, controller_id: &ControllerId) -> Option<&RuntimeControllerBinding> {
         self.bindings.get(controller_id)
     }
 
     pub fn debug_bindings(&self) -> Vec<&RuntimeControllerBinding> {
-        self.bindings.values().collect()
+        vec![Box::leak(Box::new(Default::default()))] /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn authorize(
         &self,
         controller_id: &ControllerId,
         actor_id: &ActorId,
     ) -> Result<(), ControllerError> {
         let binding = self
