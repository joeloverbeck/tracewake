--- crates/tracewake-core/src/agent/actor_known.rs
+++ replace ActorKnownPlanningContext::actor_id -> &ActorId with Box::leak(Box::new(Default::default()))
@@ -330,17 +330,17 @@
             known_workplaces: derived.known_workplaces,
             facts,
         };
         context.sort_facts();
         context
     }
 
     pub fn actor_id(&self) -> &ActorId {
-        &self.actor_id
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn current_place_id(&self) -> &PlaceId {
         &self.current_place_id
     }
 
     pub fn known_edges(&self) -> &BTreeMap<PlaceId, BTreeSet<PlaceId>> {
         &self.known_edges
