--- crates/tracewake-core/src/view_models.rs
+++ replace ActionAvailability::debug_only_diagnostics -> &[String] with Vec::leak(vec!["xyzzy".into()])
@@ -311,23 +311,17 @@
             Self::Available => &[],
             Self::Disabled {
                 provenance_refs, ..
             } => provenance_refs,
         }
     }
 
     pub fn debug_only_diagnostics(&self) -> &[String] {
-        match self {
-            Self::Available => &[],
-            Self::Disabled {
-                debug_only_diagnostics,
-                ..
-            } => debug_only_diagnostics,
-        }
+        Vec::leak(vec!["xyzzy".into()]) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
 pub struct ActionAvailabilityProvenance {
     pub kind: ActionAvailabilityProvenanceKind,
     pub reference: String,
 }
