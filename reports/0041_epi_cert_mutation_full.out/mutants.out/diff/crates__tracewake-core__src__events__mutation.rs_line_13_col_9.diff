--- crates/tracewake-core/src/events/mutation.rs
+++ replace WorldMutationCapability::mint -> Self with Default::default()
@@ -5,17 +5,17 @@
 
 #[derive(Clone, Copy, Debug)]
 pub struct AgentMutationCapability {
     _private: (),
 }
 
 impl WorldMutationCapability {
     pub(in crate::events) fn mint() -> Self {
-        Self { _private: () }
+        Default::default() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl AgentMutationCapability {
     pub(in crate::events) fn mint() -> Self {
         Self { _private: () }
     }
 }
