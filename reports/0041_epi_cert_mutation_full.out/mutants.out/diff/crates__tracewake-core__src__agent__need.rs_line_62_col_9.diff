--- crates/tracewake-core/src/agent/need.rs
+++ replace NeedBand::parse -> Result<Self, NeedParseError> with Ok(Default::default())
@@ -54,23 +54,17 @@
             0..=249 => Self::Comfortable,
             250..=499 => Self::Rising,
             500..=749 => Self::Urgent,
             _ => Self::Severe,
         }
     }
 
     fn parse(value: &str) -> Result<Self, NeedParseError> {
-        match value {
-            "comfortable" => Ok(Self::Comfortable),
-            "rising" => Ok(Self::Rising),
-            "urgent" => Ok(Self::Urgent),
-            "severe" => Ok(Self::Severe),
-            _ => Err(NeedParseError::InvalidNeedBand),
-        }
+        Ok(Default::default()) /* ~ changed by cargo-mutants ~ */
     }
 }
 
 #[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
 pub enum ThresholdDirection {
     IncreasingPressure,
     DecreasingPressure,
 }
