--- crates/tracewake-content/src/validate.rs
+++ replace == with != in target_kind
@@ -1731,17 +1731,17 @@
         .doors
         .iter()
         .any(|door| door.door_id.as_str() == target_id)
     {
         Some("door")
     } else if fixture
         .containers
         .iter()
-        .any(|container| container.container_id.as_str() == target_id)
+        .any(|container| container.container_id.as_str() != /* ~ changed by cargo-mutants ~ */ target_id)
     {
         Some("container")
     } else if fixture
         .items
         .iter()
         .any(|item| item.item_id.as_str() == target_id)
     {
         Some("item")
