--- crates/tracewake-core/src/agent/perception.rs
+++ replace record_current_place_perception_and_project -> Vec<EventEnvelope> with vec![]
@@ -36,22 +36,17 @@
     log: &mut EventLog,
     state: &mut PhysicalState,
     _agent_state: &mut AgentState,
     epistemic_projection: &mut EpistemicProjection,
     actor_id: &ActorId,
     decision_tick: SimTick,
     content_manifest_id: &ContentManifestId,
 ) -> Vec<EventEnvelope> {
-    let events =
-        record_current_place_perception(log, state, actor_id, decision_tick, content_manifest_id);
-    for event in &events {
-        project_perception_event(epistemic_projection, event);
-    }
-    events
+    vec![] /* ~ changed by cargo-mutants ~ */
 }
 
 pub fn current_place_perception_events(
     state: &PhysicalState,
     actor_id: &ActorId,
     decision_tick: SimTick,
     content_manifest_id: &ContentManifestId,
 ) -> Vec<EventEnvelope> {
