--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace KnowledgeContext::bound_actor_id -> &ActorId with Box::leak(Box::new(Default::default()))
@@ -1023,17 +1023,17 @@
         }
     }
 
     pub fn viewer_actor_id(&self) -> &ActorId {
         &self.viewer_actor_id
     }
 
     pub fn bound_actor_id(&self) -> &ActorId {
-        &self.bound_actor_id
+        Box::leak(Box::new(Default::default())) /* ~ changed by cargo-mutants ~ */
     }
 
     pub fn mode(&self) -> ViewMode {
         self.mode
     }
 
     pub fn current_tick(&self) -> SimTick {
         self.current_tick
