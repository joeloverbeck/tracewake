--- crates/tracewake-core/src/epistemics/knowledge_context.rs
+++ replace ActorKnownLocalActorFact::canonical_key -> String with "xyzzy".into()
@@ -611,17 +611,17 @@
         &self.actor_id
     }
 
     pub fn source_key(&self) -> &str {
         &self.source_key
     }
 
     fn canonical_key(&self) -> String {
-        format!("{}:{}", self.actor_id.as_str(), self.source_key)
+        "xyzzy".into() /* ~ changed by cargo-mutants ~ */
     }
 }
 
 impl ActorKnownRouteFact {
     pub fn new(
         from_place_id: PlaceId,
         to_place_id: PlaceId,
         source_key: impl Into<String>,
