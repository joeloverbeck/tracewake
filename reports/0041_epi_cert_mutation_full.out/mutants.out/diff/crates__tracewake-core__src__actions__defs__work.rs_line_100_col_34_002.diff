--- crates/tracewake-core/src/actions/defs/work.rs
+++ replace > with >= in build_work_start_events
@@ -92,17 +92,17 @@
             proposal,
             ordering_key,
             content_manifest_id,
             workplace,
             "need",
             "hunger state missing for work",
         )]);
     };
-    if i32::from(current_hunger) > workplace.max_hunger_to_start {
+    if i32::from(current_hunger) >= /* ~ changed by cargo-mutants ~ */ workplace.max_hunger_to_start {
         return Ok(vec![work_failed_event(
             proposal,
             ordering_key,
             content_manifest_id,
             workplace,
             "need",
             "hunger too high for work",
         )]);
