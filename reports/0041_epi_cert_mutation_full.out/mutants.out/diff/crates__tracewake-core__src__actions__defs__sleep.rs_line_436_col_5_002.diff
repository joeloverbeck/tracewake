--- crates/tracewake-core/src/actions/defs/sleep.rs
+++ replace parse_duration_ticks -> Result<Option<u64>, ActionRejection> with Ok(Some(1))
@@ -428,47 +428,17 @@
                 need_kind.stable_id()
             ),
         },
         current,
     ))
 }
 
 fn parse_duration_ticks(proposal: &Proposal) -> Result<Option<u64>, ActionRejection> {
-    let duration_ticks = proposal
-        .parameters
-        .get("duration_ticks")
-        .map(|value| value.parse::<u64>())
-        .transpose()
-        .map_err(|_| {
-            ActionRejection::new(
-                PipelineStage::CostDurationCheck,
-                ReasonCode::InvalidParameter,
-                vec![CheckedFact::new(
-                    "duration_ticks",
-                    proposal
-                        .parameters
-                        .get("duration_ticks")
-                        .cloned()
-                        .unwrap_or_default(),
-                )],
-                "That sleep duration is invalid.",
-                "sleep duration_ticks parameter was not an unsigned integer",
-            )
-        })?;
-    if duration_ticks == Some(0) {
-        return Err(ActionRejection::new(
-            PipelineStage::CostDurationCheck,
-            ReasonCode::InvalidParameter,
-            vec![CheckedFact::new("duration_ticks", "0")],
-            "That sleep duration is invalid.",
-            "sleep duration must be greater than zero",
-        ));
-    }
-    Ok(duration_ticks)
+    Ok(Some(1)) /* ~ changed by cargo-mutants ~ */
 }
 
 #[cfg(test)]
 mod tests {
     use std::collections::BTreeMap;
 
     use super::*;
     use crate::actions::proposal::ProposalOrigin;
