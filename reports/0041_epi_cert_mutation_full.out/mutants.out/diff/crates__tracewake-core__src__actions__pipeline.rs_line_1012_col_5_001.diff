--- crates/tracewake-core/src/actions/pipeline.rs
+++ replace actor_visible_facts -> Vec<CheckedFact> with vec![Default::default()]
@@ -1004,21 +1004,17 @@
         reason_codes,
         checked_facts,
         actor_visible_summary,
         debug_summary,
     )))
 }
 
 fn actor_visible_facts(checked_facts: &[CheckedFact]) -> Vec<CheckedFact> {
-    checked_facts
-        .iter()
-        .filter(|fact| is_actor_visible_fact(fact))
-        .cloned()
-        .collect()
+    vec![Default::default()] /* ~ changed by cargo-mutants ~ */
 }
 
 fn debug_only_facts(checked_facts: &[CheckedFact]) -> Vec<CheckedFact> {
     checked_facts
         .iter()
         .filter(|fact| !is_actor_visible_fact(fact))
         .cloned()
         .collect()
