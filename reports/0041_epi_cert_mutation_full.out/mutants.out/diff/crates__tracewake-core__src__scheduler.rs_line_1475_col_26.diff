--- crates/tracewake-core/src/scheduler.rs
+++ replace == with != in no_human::append_passive_need_events_before_decision
@@ -1467,17 +1467,17 @@
 
     fn append_passive_need_events_before_decision(
         state: &PhysicalState,
         log: &mut EventLog,
         agent_state: &mut AgentState,
         context: &PassiveNeedDecisionContext<'_>,
         elapsed_ticks: u64,
     ) {
-        if elapsed_ticks == 0 {
+        if elapsed_ticks != /* ~ changed by cargo-mutants ~ */ 0 {
             return;
         }
         let previous_tick = SimTick::new(
             context
                 .window
                 .start_tick
                 .value()
                 .saturating_sub(elapsed_ticks),
