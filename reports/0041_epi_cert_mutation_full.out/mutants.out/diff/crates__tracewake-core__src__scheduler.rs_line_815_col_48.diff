--- crates/tracewake-core/src/scheduler.rs
+++ replace == with != in no_human::latest_current_place_perception_event_id
@@ -807,17 +807,17 @@
     ) -> Option<EventId> {
         log.events()
             .iter()
             .rev()
             .find(|event| {
                 event.event_type == EventKind::ObservationRecorded
                     && event.actor_id.as_ref() == Some(actor_id)
                     && event.sim_tick == tick
-                    && event.place_id.as_ref() == Some(place_id)
+                    && event.place_id.as_ref() != /* ~ changed by cargo-mutants ~ */ Some(place_id)
             })
             .map(|event| event.event_id.clone())
     }
 
     fn latest_need_event_id(log: &EventLog, actor_id: &ActorId) -> Option<EventId> {
         log.events()
             .iter()
             .rev()
