--- crates/tracewake-core/src/agent/planner.rs
+++ delete ! in plan_route
@@ -229,17 +229,17 @@
         expansions += 1;
         let neighbors = state
             .known_edges()
             .get(&place_id)
             .cloned()
             .unwrap_or_default();
         for neighbor in neighbors {
             candidates.push(format!("edge:{}->{}", place_id.as_str(), neighbor.as_str()));
-            if !visited.insert(neighbor.clone()) {
+            if  /* ~ changed by cargo-mutants ~ */visited.insert(neighbor.clone()) {
                 continue;
             }
             let mut next_path = path.clone();
             next_path.push(neighbor.clone());
             if &neighbor == target {
                 let proposals = proposals_for_path(state, state.current_place_id(), &next_path);
                 return Ok(LocalPlan {
                     trace: trace(
