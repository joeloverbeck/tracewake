# 0036P0CERPOS0008-004: P0-03 evidence — human-origin commands bind to ordinary actors and share autonomous action rules

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-03 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 3 (spec §7.3): human-origin commands must bind to ordinary actors and traverse the same action rules as autonomous actors — input origin appears as proposal metadata, not as privileged action semantics. This ticket gathers possession-parity witnesses and the negatives proving no player privilege, and writes the P0-03 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-tui/src/{input,app,render}.rs`, `tracewake-core/src/controller.rs`, `actions/{proposal,pipeline,registry}.rs`, `actions/defs/mod.rs`, `view_models.rs`, `state.rs`.
2. Spec §7.3 names the corpus; `possession_parity_001.rs`, `view_model_local_actions_001.rs` exist, and the TUI suites it hedges "if present" — `command_loop_session.rs`, `embodied_flow.rs`, `tui_acceptance.rs`, `tui_seam_conformance.rs` — are all present at HEAD (confirmed this session) and folded into scope.
3. Shared boundary under audit: the TUI input→controller→proposal→pipeline path versus the autonomous decision path. The witness pairs one human-origin command and one analogous autonomous decision and shows both produce ordinary proposals through the same validation and event-append seams.
4. `INV-005` (only binding is ordinary possession), `INV-007` (every world-affecting player action must be NPC-possible), and `INV-008` (UI assistance is not authority) motivate this ticket; `INV-043` (validation does not branch for player privilege) is the enforcement intent.
5. This ticket audits the possession-binding and view-model surfaces as an evidence consumer. It runs `possession_parity_001`, `view_model_local_actions_001`, the TUI suites, and the debug-attach negatives, recording that a possessed actor imports no player omniscience, debug truth, or fixture truth into memory/beliefs. It weakens no enforcement; a mutation granting the possessed actor privileged action defs, hidden knowledge, or direct event-append authority must be killed (recorded `fail` if surviving).

## Architecture Check

1. Pairing a human command with an analogous autonomous decision and showing identical pipeline traversal is stronger than asserting parity: it proves the only difference is proposal origin metadata (spec §7.3).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-007` NPC-possible -> replay/golden-fixture check: `possession_parity_001` shows possessed and autonomous actors share ordinary world rules.
2. `INV-008` UI is not authority -> codebase grep-proof + manual review: TUI submits typed attempts only; `view_model_local_actions_001` derives the menu from embodied state, not truth.
3. No-direct-dispatch from TUI -> codebase grep-proof: direct-dispatch rejection tests fail closed; debug attach does not alter candidate actions/beliefs.

## What to Change

### 1. Capture the P0-03 parity witness

For at least one human-origin command and one analogous autonomous decision, record that both produce ordinary proposals, traverse the same action validation and event append seams, and replay to equivalent world effects when context permits — with input origin appearing only as proposal origin metadata.

### 2. Record actor-knowledge and debug-quarantine evidence

Show the human command binds to the actor's ordinary holder-known context and actor-visible affordance set (or why it is a direct ordinary action constrained by embodied affordance, not cognition), importing no player omniscience/debug/fixture truth. If debug panels are active, show their capability stays separate from the possessed actor's menu and embodied view. Map failures to typed diagnostics from {`tui_input_binding`, `view_model`, `proposal_construction`, `action_validation`, `event_append`, `debug_quarantine`, `test_oracle`}.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Possession non-reset of needs/intentions/memories (P0-04, owned by `-005`).
- Debug quarantine carrier census (P0-07, owned by `-008`) beyond what P0-03 requires.
- Remediation of any privilege/bypass found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `possession_parity_001` — possessed and autonomous actor share ordinary world rules.
2. `view_model_local_actions_001` — actor-visible local actions derived from embodied state.
3. TUI suites (`command_loop_session`, `embodied_flow`, `tui_acceptance`, `tui_seam_conformance`) pass.
4. `possession_does_not_reset_intention_001`, `debug_attach_001`, `debug_omniscience_excluded_001` — human binding persists; debug attach is quarantined.

### Invariants

1. No human command invokes an action unavailable to autonomous actors under the same world facts (`INV-007`/`INV-043`).
2. No debug/player truth enters actor knowledge via possession (`INV-008`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing possession/TUI suites and the captured parity witness is the deliverable.`

### Commands

1. `cargo test -p tracewake-tui --test tui_acceptance`
2. `cargo test -p tracewake-tui --test tui_seam_conformance && cargo test -p tracewake-tui --test command_loop_session`
3. `cargo test -p tracewake-content --test golden_fixtures_run` (exercises `possession_parity_001` / `view_model_local_actions_001`)
