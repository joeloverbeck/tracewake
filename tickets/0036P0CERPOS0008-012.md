# 0036P0CERPOS0008-012: Capstone — consolidate replay/provenance, sampling, pending/historical, certification-use, staged abstractions, and render the verdict

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — assembles the cross-gate acceptance fields and verdict; introduces no production logic
**Deps**: 0036P0CERPOS0008-002, 0036P0CERPOS0008-003, 0036P0CERPOS0008-004, 0036P0CERPOS0008-005, 0036P0CERPOS0008-006, 0036P0CERPOS0008-007, 0036P0CERPOS0008-008, 0036P0CERPOS0008-009, 0036P0CERPOS0008-010, 0036P0CERPOS0008-011

## Problem

Spec §8 requires a single `P0-CERT` acceptance artifact with cross-gate consolidation fields and exactly one verdict. With the ten gate sections (`-002`…`-011`) and the scaffold/transcript (`-001`) in place, this capstone consolidates the replay/provenance section (§8.5), sampling/exhaustiveness (§8.6), pending/historical (§8.7), certification-use (§8.8), and staged-abstraction declarations (§8.9), records the tolerated deferrals (§10), runs the §12 implementer self-check, and renders the §8.1 verdict — `P0-CERT passed` or `P0-CERT scoped remediation` (never `not applicable`, since the spec targets live code). It introduces no new production logic; it exercises and assembles what the prior tickets composed.

## Assumption Reassessment (2026-06-16)

1. The artifact and its ten gate sections exist once `-001`…`-011` land (gate tickets each `Deps: -001`; this capstone `Deps` the full gate leaf set `-002`…`-011`, transitively covering `-001`). The §8 field shapes come from `docs/4-specs/0003_ACCEPTANCE_ARTIFACT_TEMPLATE.md` (verified present this session), whose evidence-status (§8.2) and staged-abstraction-declaration fields match the spec — the `0003` staged-abstraction area was added by archived spec `0035` (per `SPEC_LEDGER.md`).
2. Spec §8.9 enumerates the minimum staged-abstraction declarations; §10 enumerates the tolerated deferrals (PHASE-4-ENTRY, SECOND-PROOF-ENTRY, LLM/speech, inventory/economy, EMERGE-OBS, temporal-firewall). §12 enumerates the closing self-check. All resolve against the live spec.
3. Cross-artifact boundary under audit: the single-verdict honesty rule — `pending`/`sampled`/`observer-only`/`historical` evidence may never be silently counted as `pass` (§8.2), and the verdict scope is `post-0008 baseline only`.
4. `INV-098` (feature acceptance is harsh — done only when caused, agent-possible, eventful, trace-aware, epistemically bounded, TUI-playable, debug-inspectable, no-human runnable, replay-safe, LLM-independent, non-scripted, regression-tested), `INV-111` (observer-only emergence evidence), and `INV-112` (time may validate, but holder-known time must plan) motivate this ticket.
5. This ticket consolidates replay/provenance, possession, actor-knowledge, and EMERGE-OBS evidence as a review/assembly artifact. It introduces no enforcement and weakens none: it confirms the §8.5 divergence report holds within declared scope, that EMERGE-OBS rows stay observer-only/non-gating (`INV-111`), and that temporal evidence stays first-proof-scoped and certifies no Phase-4 procedural time or second-proof LOD/time-acceleration by implication (`INV-112`). The verdict is a recorded outcome, not a simulation input.

## Architecture Check

1. A single trailing capstone that assembles the cross-gate fields and renders one verdict is cleaner than letting any gate ticket assert a project-level result: it keeps the single-artifact honesty rule (§8.2) and the single-verdict scope in one reviewable place, exercising — not modifying — the prior tickets' evidence.
2. No backwards-compatibility aliasing or shims introduced; the verdict cites the gates consumed and mints no new gate-code/status vocabulary.

## Verification Layers

1. `INV-098` harsh acceptance -> manual review + invariants alignment check: §12 self-check confirms all ten gates have evidence-status entries and the acceptance criteria below are met before a `pass` verdict.
2. `INV-111` EMERGE-OBS observer-only -> invariants alignment check: the §8.9 EMERGE-OBS declaration confirms it is non-gating, non-threshold, replay-derived, quarantined from simulation inputs.
3. `INV-112` temporal authority -> invariants alignment check: the temporal staged-abstraction declaration confirms first-proof temporal evidence certifies no Phase-4 procedural time / second-proof LOD by implication.
4. Replay/provenance consolidation -> replay/golden-fixture check: §8.5 divergence report (or explicit no-divergence with checked scope) holds, citing `-001`'s global fingerprints and the gate tickets' provenance rows.

## What to Change

### 1. Consolidate the cross-gate acceptance fields

Assemble §8.5 (replay/provenance: manifest fingerprint + schema version, seeds/random-stream IDs, event-log fingerprint, live + replay projection checksums, divergence/no-divergence report, actor-known provenance rows for selected actions and rejected hidden-truth cases, observer-only debug truth/belief rows, at least one negative provenance case failing closed); §8.6 (sampling/exhaustiveness: distinguish exhaustive seam audits from sampled/golden witnesses from property/generative evidence with recorded seeds); §8.7 (pending/historical: archive references `historical`, unavailable/unsupported mutation or incomplete property evidence as `pending`/`fail`, remediation-required findings by gate + layer).

### 2. Declare staged abstractions and tolerated deferrals

Author the §8.9 staged-abstraction declarations for at minimum the five enumerated: `EMERGE-OBS`; no-human canonical corpus sampling (if not exhaustive); mutation testing (if sampled/unsupported/non-exhaustive); TUI transcript evidence (if sampled); temporal evidence in first-proof surfaces (must not certify Phase-4 procedural time or second-proof LOD/time-acceleration). Record the §10 tolerated deferrals tied to their named entry gates (PHASE-4-ENTRY, SECOND-PROOF-ENTRY, LLM/speech boundary, inventory/economy, EMERGE-OBS, temporal-firewall).

### 3. Author certification-use and render the verdict

Author §8.8 (what later specs may cite, the stricter entry gates they still must satisfy, the no-EMERGE-OBS-threshold and no-archive-as-live rules). Render the §8.1 verdict (`P0-CERT passed` | `P0-CERT scoped remediation`), verdict scope `post-0008 baseline only`. Complete the §12 implementer self-check.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Per-gate evidence collection (owned by `-002`…`-011`) and the command transcript/scaffold (owned by `-001`); the capstone exercises and consolidates them, it does not re-collect.
- The `docs/4-specs/SPEC_LEDGER.md` 0036 row and the `archive/specs/` move — deferred to spec acceptance/archival (Step 6 cross-spec follow-up).
- Any remediation work a `scoped remediation` verdict implies — routed to a separate `P0-CERT scoped remediation` spec/ticket; this capstone only names the failed gate + responsible layer.

## Acceptance Criteria

### Tests That Must Pass

1. Re-enumerate from the artifact at assembly time (not hardcoded): all ten P0-CERT requirements (§7.1–§7.10) have evidence-status entries.
2. `cargo test --workspace --locked` passes (or every failure is recorded `fail` with responsible layer and reflected in the verdict).
3. §8.5 divergence report holds within declared fingerprint scope, or a divergence is recorded and reflected in the verdict.
4. Each of the five minimum staged-abstraction declarations (§8.9) is present and complete (status, proves-now, abstracts, must-not-fake, future-tier-not-certified, leak-diagnostic).

### Invariants

1. No `pending`/`sampled`/`observer-only`/`historical` evidence is counted as `pass`; verdict scope is `post-0008 baseline only` (`INV-098`; §8.2).
2. `EMERGE-OBS` is observer-only/non-gating and temporal evidence certifies no Phase-4/second-proof surface by implication (`INV-111`/`INV-112`).
3. No pass/fail result relies on the spec's existence rather than this generated acceptance artifact.

## Test Plan

### New/Modified Tests

1. `None — verification-only capstone; it assembles the acceptance artifact from the prior tickets' evidence and runs no new production logic. Verification is the §12 self-check plus a full-workspace test pass.`

### Commands

1. `cargo test --workspace --locked` (full-pipeline acceptance — every gate's underlying tests)
2. `for s in "P0-01" "P0-02" "P0-03" "P0-04" "P0-05" "P0-06" "P0-07" "P0-08" "P0-09" "P0-10"; do grep -q "$s" archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md || echo "MISSING $s"; done` (all ten gate sections present)
3. `grep -nE "Verdict: (P0-CERT passed|P0-CERT scoped remediation)" archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (exactly one rendered verdict)
