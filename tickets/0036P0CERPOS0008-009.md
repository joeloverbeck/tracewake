# 0036P0CERPOS0008-009: P0-08 evidence — golden fixtures include adversarial hidden-truth, no-human, possession, replay, view-model, content-validation, and direct-dispatch rejection cases

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-08 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 8 (spec §7.8): the fixture corpus must cover both friendly and hostile families, every golden counted as certification must have a semantic behavior witness (not golden bytes alone), and every negative must fail for its intended responsible layer. This ticket audits the corpus for coverage and semantic fidelity, then writes the P0-08 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-content/src/fixtures/mod.rs`, `schema.rs`, `validate.rs`, `serialization.rs`, `load.rs`; `tracewake-content/tests/{golden_fixtures_run,forbidden_content,schema_conformance}.rs`; `tracewake-core/tests/{golden_scenarios,negative_fixture_runner,acceptance_gates,hidden_truth_gates,event_schema_replay_gates}.rs`; `tracewake-tui/tests/adversarial_gates.rs`.
2. Spec §7.8 names the corpus; `schema_conformance.rs` (hedged "if present") is present at HEAD (confirmed this session). All named positive and negative fixtures exist.
3. Shared boundary under audit: the fixture-contract surface — each golden must list expected holder-known context facts and prohibited hidden facts, with actor-known provenance from modeled sources, not fixture prose/comments/labels/debug fields.
4. `INV-060` (no authored outcome chains), `INV-091` (no-human tests mandatory), `INV-092` (deterministic replay tested), and `INV-097` (no-script compliance tested) motivate this ticket; the missing-property proof must carry no authorial outcome chain.
5. This ticket audits the fixture/content-validation surface as an evidence consumer. It runs the positive families and the negative families, recording for each golden a semantic witness plus stable serialization data, and for each negative that it failed for the intended layer (not incidental parse/panic/unrelated validation). It weakens no enforcement; this gate is the relapse guard against friendly-only proof (spec §11.1 #1).

## Architecture Check

1. Requiring both a semantic behavior witness and a stable fingerprint per golden — and an intended-layer failure reason per negative — is stronger than counting fixtures: it resists the friendly-only and replay-byte-fetish relapses (spec §11.1 #1/#7).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-092` replay tested -> replay/golden-fixture check: `replay_item_location_001` + `event_schema_replay_gates` show byte stability paired with a semantic witness.
2. `INV-060`/`INV-097` no-script / no authored outcome -> schema validation: `forbidden_content`, `schema_conformance`, `prose_born_fact_rejected_001` reject outcome chains, prose-born facts, debug/omniscience fields.
3. Corpus completeness -> manual review: representative positives and negatives present for every family (hidden-truth, no-human, possession, replay, view-model, content-validation, direct-dispatch).

## What to Change

### 1. Audit positive families with semantic witnesses

Record representative positives with semantic witnesses (not golden bytes alone) from: hidden-truth resistance (`no_hidden_truth_planning_001`, `seeded_food_source_unknown_to_all_actors_001`); no-human (`no_human_day_001`, `ordinary_workday_001`, `sleep_eat_work_001`); possession (`possession_parity_001`, `possession_does_not_reset_intention_001`); replay (`replay_item_location_001`, event schema/replay tests); view-model (`view_filtering_001`, `view_model_local_actions_001`); content validation (`schema_conformance`, load tests); direct-dispatch (acceptance/no-direct tests).

### 2. Audit negative families with intended-layer failure reasons

Record negatives failing closed for the intended layer: `hidden_food_closed_container_001`, `hidden_route_edge_001`, `hidden_truth_audit_rejects_typed_unproven_fact_without_banned_words_001`, `forbidden_provenance_input_fails_closed_001`, `prose_born_fact_rejected_001`, `debug_omniscience_excluded_001`, `embodied_view_omits_raw_assignment_without_context_001`, `embodied_view_omits_unobserved_food_at_open_place_001`, `scheduler_cannot_rewrite_wait_reason_after_transaction_001`, plus direct-dispatch rejection and forbidden-API negatives. For cognition fixtures, list expected holder-known facts and prohibited hidden facts. Map failures to typed diagnostics from {`fixture_contract`, `content_schema`, `content_validation`, `holder_known_context`, `test_oracle`, `view_model`, `debug_quarantine`, `replay`, or the action/scheduler layer under test}.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Per-gate deep witnesses owned by `-002`…`-008` and `-010`/`-011`; this gate audits corpus coverage and semantic fidelity, citing those gates' witnesses rather than reproducing them.
- Remediation of any missing family / friendly-only golden found — recorded `fail`; fix (new fixtures) routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `cargo test -p tracewake-content --test golden_fixtures_run` and `cargo test -p tracewake-core --test golden_scenarios` — golden positives run with semantic witnesses.
2. `cargo test -p tracewake-content --test forbidden_content` and `cargo test -p tracewake-content --test schema_conformance` — content-validation negatives reject outcome chains / prose-born / debug fields.
3. `cargo test -p tracewake-core --test negative_fixture_runner` and `cargo test -p tracewake-tui --test adversarial_gates` — negatives fail for intended layer.

### Invariants

1. Every golden counted as certification carries a semantic behavior witness, not golden bytes alone (`INV-092`).
2. The corpus includes hidden-truth adversarial and direct-dispatch negative cases; no family is friendly-only (`INV-097`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing positive/negative fixture suites and the captured coverage+witness audit is the deliverable.`

### Commands

1. `cargo test -p tracewake-content` (golden_fixtures_run + forbidden_content + schema_conformance + fixtures_load)
2. `cargo test -p tracewake-core --test negative_fixture_runner && cargo test -p tracewake-tui --test adversarial_gates`
3. `cargo test -p tracewake-core --test event_schema_replay_gates` (replay golden semantic + byte pairing)
