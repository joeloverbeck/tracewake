# 0036P0CERPOS0008-001: Acceptance-artifact scaffold and full command/lock-layer transcript

**Status**: PENDING
**Priority**: HIGH
**Effort**: Large
**Engine Changes**: None — produces the `archive/reports/` P0-CERT acceptance artifact and embedded/linked command transcripts; runs existing tests only, changes no engine code
**Deps**: None

## Problem

Spec 0036 requires the implementing session to produce a single `P0-CERT` acceptance artifact that proves the post-0008 baseline seam by seam (§8). Before any per-gate evidence section can be filled, the artifact must exist with its header, evidence-status legend, fingerprint-scope legend, and the cross-gate command/lock-layer transcript that the whole audit rests on. This ticket creates that scaffold and runs the §8.10 command set plus the global replay/provenance fingerprints (§8.5), so the ten gate tickets (`-002`…`-011`) and the capstone (`-012`) have a populated, honest foundation to write into.

This ticket renders **no verdict** — the verdict is the capstone's (`-012`). It establishes evidence infrastructure only.

## Assumption Reassessment (2026-06-16)

1. The acceptance-artifact target path `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` does not yet exist (`test -f` → absent, 2026-06-16); its parent `archive/reports/` exists. This ticket creates the file `(new)`.
2. Spec §8.1–§8.3 fix the header fields, the evidence-status vocabulary (`pass|fail|pending|sampled|observer-only|historical`), and the fingerprint-scope vocabulary; §8.10 enumerates the required command transcript; §8.5 enumerates the global replay/provenance fields. The artifact must conform to `docs/4-specs/0003_ACCEPTANCE_ARTIFACT_TEMPLATE.md` (verified present 2026-06-16), whose evidence-status and fingerprint-scope fields match §8.2/§8.3 (the §8.3 scope list extends `0003` with `context hash/frontier`, `projection checksum`, `event log checksum`).
3. Shared boundary under audit: the acceptance artifact contract itself — the `0003` template fields and the §8.2 evidence-status discipline that every sibling gate ticket writes against. The legends authored here are the contract all of `-002`…`-012` consume; their wording must match `0003` and §8.2/§8.3 exactly so sibling sections stay consistent.
4. `INV-017` (randomness seedable/auditable) and `INV-018` (deterministic replay is foundational) motivate this ticket: the §8.10 transcript and §8.5 fingerprints exist to make replay/determinism evidence reproducible. Recorded seeds and random-stream identifiers accompany any generated/property case (spec §4.4).
5. This ticket touches deterministic-replay and fail-closed-validation surfaces as an **evidence consumer**, not a modifier: it runs `crates/tracewake-core/tests/event_schema_replay_gates.rs`, `crates/tracewake-content/src/fixtures/replay_item_location_001.rs`, the content positive/negative validation paths, and the lock-layer guard suites, then records their output. The artifact's debug-derived rows (truth/belief comparisons, no-human metrics) are labeled `observer-only` per §8.2 and INV-107; no transcript capture becomes diegetic actor knowledge or feeds simulation. No enforcement surface is weakened — this ticket adds none.

## Architecture Check

1. A single scaffold-and-transcript ticket landing first is cleaner than letting each gate ticket re-run the full command set: the expensive `cargo` build/test/mutation/replay pass runs once, its transcript is cited by all gates, and the legends are authored in one place so sibling sections cannot drift in evidence-status or fingerprint-scope wording.
2. No backwards-compatibility aliasing or shims. The artifact is new; it introduces no alternate path to any existing surface.

## Verification Layers

1. `INV-018` deterministic replay -> replay/golden-fixture check: `event_schema_replay_gates.rs` + `replay_item_location_001.rs` run; live vs replay projection checksums recorded with declared fingerprint scope.
2. `INV-017` seedable randomness -> manual review: recorded seeds / random-stream IDs present for any generated/property case (`generative_lock.rs`).
3. Evidence-honesty discipline -> manual review + invariants alignment check: every transcript row carries exactly one §8.2 status; `pending`/`sampled`/`observer-only`/`historical` never silently counted as `pass`.
4. Command-set completeness -> codebase grep-proof: each §8.10 command resolves and its transcript is captured (enumerated in Acceptance Criteria).

## What to Change

### 1. Create the acceptance artifact with header and legends

Create `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` with the §8.1 header (Spec, Repository, Target commit `9f1622244c91c5952bd735da76f29fbe58f39f4b`, Freshness claim, `Verdict: <pending>`, Verdict scope, Archived-evidence posture `historical only`, EMERGE-OBS posture `observer-only, non-gating, non-threshold`), the §8.2 evidence-status legend, the §8.3 fingerprint-scope legend (including the three P0-CERT-specific scopes), and empty section headers for each of the ten gates (P0-01…P0-10) plus the capstone consolidation fields (§8.4–8.9), so `-002`…`-012` fill their own sections.

### 2. Run and capture the §8.10 command/lock-layer transcript

Run and capture transcripts for: `cargo fmt --all --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo build --workspace --all-targets --locked`; `cargo test --workspace --locked`; the lock-layer gate tests (`crates/tracewake-core/tests/anti_regression_guards.rs`, `ci_workflow_guards.rs`, `spine_conformance.rs`, `hidden_truth_gates.rs`, `event_schema_replay_gates.rs`); configured mutation testing via `.cargo/mutants.toml` (or a labeled `pending`/remediation record if unavailable); replay/golden fixture runs with checksum outputs; content validation positive + negative runs; TUI possession/debug-quarantine runs or transcript snapshots; and the static guard scans for forbidden direct dispatch, hidden-truth access, debug leakage, string-typed diagnostics, branch-on-human privilege, unordered iteration, wall-clock time, and unscoped randomness.

### 3. Record the §8.5 global replay/provenance fingerprints

Record: fixture manifest fingerprint + schema version; run seed / random-stream IDs; event-log serialization fingerprint; live projection checksum; replay projection checksum; divergence report (or explicit no-divergence report with checked scope). Each fingerprint declares its §8.3 scope.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (new)
- transcript/checksum capture files under `archive/reports/` (new — implementation-discovered set; embed in the artifact or link as machine-readable siblings per §8.10)

## Out of Scope

- Per-gate evidence sections P0-01…P0-10 (owned by `-002`…`-011`).
- Capstone consolidation, staged-abstraction declarations, and the final verdict (owned by `-012`).
- Remediation of any command/lock-layer failure surfaced here — routed to a separate `P0-CERT scoped remediation` ticket/spec (spec §11.2 #7); this ticket records the failure with status `fail` and responsible layer, it does not fix it.

## Acceptance Criteria

### Tests That Must Pass

1. `cargo fmt --all --check` — transcript captured with pass/fail status.
2. `cargo clippy --workspace --all-targets -- -D warnings` — transcript captured.
3. `cargo build --workspace --all-targets --locked` — transcript captured.
4. `cargo test --workspace --locked` — transcript captured.
5. Lock-layer suites run and captured: `anti_regression_guards`, `ci_workflow_guards`, `spine_conformance`, `hidden_truth_gates`, `event_schema_replay_gates`.
6. Mutation posture run via `.cargo/mutants.toml`, or recorded as `pending`/remediation with a stated reason.
7. Replay/golden fixture runs captured with live vs replay projection checksums and a divergence (or no-divergence) report.
8. Content validation positive + negative runs captured.
9. TUI possession/debug-quarantine run or transcript snapshot captured.
10. Static guard scans (direct dispatch, hidden-truth access, debug leakage, string-typed diagnostics, branch-on-human, unordered iteration, wall-clock, unscoped randomness) run and captured.

### Invariants

1. The artifact carries the §8.1 header, §8.2 evidence-status legend, and §8.3 fingerprint-scope legend verbatim-consistent with `docs/4-specs/0003_ACCEPTANCE_ARTIFACT_TEMPLATE.md` and spec §8.2/§8.3.
2. Every recorded fingerprint declares its scope; no byte-stable golden is presented as semantic proof without a behavior witness (deferred to the owning gate ticket).
3. `Verdict` reads `<pending>` until `-012` renders it; no gate result is asserted by this ticket.

## Test Plan

### New/Modified Tests

1. `None — documentation/evidence-only ticket; verification is command-based (run the §8.10 command set and existing lock-layer/replay/content/TUI suites) and the captured transcripts are the deliverable.`

### Commands

1. `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings`
2. `cargo build --workspace --all-targets --locked && cargo test --workspace --locked`
3. `cargo test -p tracewake-core --test event_schema_replay_gates` (replay lock layer — narrower boundary for the replay/fingerprint capture)
