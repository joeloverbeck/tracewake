# 0036P0CERPOS0008-007: P0-06 evidence — validation truth may accept/reject/mutate through events but may not propose fallback plans or actor-visible hidden facts

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-06 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 6 (spec §7.6): validator truth may accept, reject, or mutate through the event path, but it may not generate fallback plans, reveal hidden facts, or insert beliefs without a modeled event. This ticket gathers the accept/reject witnesses and the negatives proving the validator never becomes a planner, and writes the P0-06 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-core/src/actions/{pipeline,report,registry}.rs`, `actions/defs/{eat,sleep,work}.rs`, `events/{mutation,apply}.rs`, `state.rs`, `projections.rs`, `agent/{transaction,trace}.rs`.
2. Spec §7.6 names the corpus; `embodied_workplace_believed_open_truth_closed_commit_fails_001`, `work_completion_fails_when_actor_displaced_001`, `sleep_interrupted_by_severe_need_prorates_recovery_001` exist, and `sleep_rejects_current_place_without_sleep_affordance_001` (hedged "if exercised") is present at HEAD (confirmed this session).
3. Shared boundary under audit: the validation pipeline as a truth-consuming acceptor/rejector versus a cognition source. The witness shows accepted validation mutates only through the event path and rejected validation yields an action report + actor-visible failure/stuck diagnostic, never a hidden-truth fallback plan.
4. `INV-099` (truth may validate, not plan) and `INV-106` (validation failure feeds replanning without leakage) motivate this ticket; the actor learns only modeled consequences of rejection.
5. This ticket audits the validation and event-application surfaces as an evidence consumer. It runs the accept/reject positives and the hidden-truth negatives, recording that validator truth may inspect reachability/presence/availability/access/invariants only *after* proposal construction and may not add candidate goals, route hints, food targets, suspects, clues, or workplace facts to actor-known context. It weakens no enforcement; a mutation turning rejection into fallback candidate generation, hidden-target selection, an actor-visible hint, or belief insertion without a modeled event must be killed (recorded `fail` if surviving).

## Architecture Check

1. Separating "validator rejects on truth" (allowed) from "validator plans on truth" (forbidden) via paired accept/reject witnesses + replay is stronger than asserting the firewall holds: it catches the validator-fallback temptation (spec §11.1 #6).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-099` truth validates not plans -> replay/golden-fixture check: `embodied_workplace_believed_open_truth_closed_commit_fails_001`, `work_completion_fails_when_actor_displaced_001` reject on truth without revealing a target.
2. `INV-106` no-leak replanning -> manual review: rejected validation surfaces only actor-available reasons; replay reproduces accept/reject outcomes and projection state.
3. No hidden-fact insertion -> codebase grep-proof: `hidden_food_closed_container_001`, `hidden_route_edge_001`, `forbidden_provenance_input_fails_closed_001` fail closed.

## What to Change

### 1. Capture accept and reject witnesses

For accepted validation, show event mutations occur only through the event path. For rejected validation, show an action report and any actor-visible failure/stuck diagnostic — not a hidden-truth fallback plan. Show replay reproduces accept/reject outcomes and projection state.

### 2. Record provenance and debug-quarantine boundaries

Show the selected action is justified by pre-validation holder-known context, and that validator truth adds no candidate goals/route hints/food targets/suspects/clues/workplace facts to actor-known context. Confirm any debug-only truth in validation diagnostics is quarantined and absent from actor-visible output unless created by a modeled event/observation. Map failures to typed diagnostics from {`action_validation`, `event_application`, `holder_known_context`, `candidate_generation`, `local_planning`, `proposal_construction`, `debug_quarantine`}.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- Actor-known autonomy provenance generally (P0-02, owned by `-003`) beyond the validation boundary.
- Remediation of any validator-fallback/leak found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `embodied_workplace_believed_open_truth_closed_commit_fails_001` — actor may believe, validator may reject on truth.
2. `work_completion_fails_when_actor_displaced_001`, `sleep_interrupted_by_severe_need_prorates_recovery_001` — truth-based rejection / interrupted-duration application.
3. `cargo test -p tracewake-core --test acceptance_gates` — action report accept/reject path.
4. Negatives fail closed: `hidden_food_closed_container_001`, `hidden_route_edge_001`, `forbidden_provenance_input_fails_closed_001`, `hidden_truth_audit_rejects_typed_unproven_fact_without_banned_words_001`.

### Invariants

1. No validator path proposes fallback plans, updates cognition, reveals hidden facts, or converts a rejection into an actor-visible target without modeled information flow (`INV-099`/`INV-106`).
2. Validator truth inspects only after proposal construction; it never adds facts to actor-known context (`INV-101`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing validation/acceptance fixtures and the captured accept/reject witnesses are the deliverable.`

### Commands

1. `cargo test -p tracewake-core --test acceptance_gates`
2. `cargo test -p tracewake-content --test golden_fixtures_run` (exercises the workplace/displacement/sleep validation fixtures)
3. `cargo test -p tracewake-core --test hidden_truth_gates` (validator-truth-cannot-become-target boundary)
