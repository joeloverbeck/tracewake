# 0036P0CERPOS0008-002: P0-01 evidence — world-affecting behavior enters through proposal→validation→event→application→projection→replay

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-01 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 1 (spec §7.1): all world-affecting behavior must enter through proposal, validation, event append, application, projection, and replay boundaries. This ticket gathers the behavior witnesses, replay/projection evidence, and direct-dispatch negatives that prove the boundary holds on the production path, and writes the P0-01 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-core/src/actions/{proposal,pipeline,report,registry}.rs`, `actions/defs/{mod,eat,sleep,work}.rs`, `controller.rs`, `events/{log,envelope,apply,mutation}.rs`, `state.rs`, `projections.rs`, `epistemics/projection.rs`, `view_models.rs`, `replay/{rebuild,report}.rs`, `checksum.rs`, `tracewake-content/src/{load,manifest}.rs`.
2. Spec §7.1 names the positive and negative corpus; all named files exist at HEAD (confirmed during this session's `/reassess-spec`). The artifact section and field shapes come from `-001`'s scaffold and `docs/4-specs/0003_ACCEPTANCE_ARTIFACT_TEMPLATE.md`.
3. Shared boundary under audit: the production action→event→replay pipeline. The witness must trace a single triggering input through `Proposal` (origin/source context) → pipeline validation report → appended envelope (schema/version/order/ancestry) → application → live projection checksum → serialized log → replay rebuild → replay projection checksum.
4. `INV-009` (meaningful changes require events), `INV-011` (current-state-only simulation forbidden), and `INV-018` (deterministic replay) motivate this ticket: a pass requires replay ancestry, never current state alone.
5. This ticket audits the deterministic-replay and event-append surfaces as an evidence consumer. It runs `event_schema_replay_gates.rs`, `replay_item_location_001.rs`, `golden_scenarios.rs`, `spine_conformance.rs`, and the direct-dispatch negatives, then records witnesses; it weakens no enforcement. Any mutation that bypasses proposal/pipeline/event/replay must be killed or triaged as remediation (recorded `fail`, not fixed here).

## Architecture Check

1. Auditing the boundary via an end-to-end behavior witness plus replay equivalence is stronger than artifact-presence checks: it proves semantics, not just stable bytes, per spec §4.2.
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-018` deterministic replay -> replay/golden-fixture check: live vs replay projection checksums compared by declared scope for `replay_item_location_001` and `golden_scenarios`.
2. `INV-009`/`INV-011` eventful, no current-state authority -> manual review + replay check: witness shows every world mutation has an appended event and replay ancestry.
3. No-direct-dispatch -> codebase grep-proof + replay/golden-fixture check: direct-dispatch rejection cases in `acceptance_gates.rs`/`hidden_truth_gates.rs`/`anti_regression_guards.rs` fail closed.

## What to Change

### 1. Capture the P0-01 behavior witness

Record one production-path witness (triggering input or scheduler decision → `Proposal` with origin/source context → pipeline validation report → appended event envelope with schema/version/order/ancestry → application result → live projection checksum → serialized log → replay rebuild report → replay projection checksum), naming fingerprint scope separately for raw bytes, normalized serialization, parsed semantic content, run seed, and replay artifact.

### 2. Record provenance, debug-quarantine, and typed-diagnostic evidence

For actor-originated proposals: holder-known context ID, hash/frontier, action-relevant fact provenance. For non-cognitive passive accounting: the allowed origin and proof it cannot produce primitive action choices. Confirm debug-derived rows are `observer-only`. Map any failure to a typed diagnostic from {`proposal_construction`, `action_validation`, `event_append`, `event_application`, `projection`, `replay`, `scheduler`, `test_oracle`}.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- The §8.10 cross-gate command transcript and global fingerprints (owned by `-001`).
- Remediation of any boundary bypass found — recorded `fail` with responsible layer; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `cargo test -p tracewake-content --test golden_fixtures_run` and `cargo test -p tracewake-core --test golden_scenarios` — ordinary sleep/eat/work + workday progression through events.
2. `cargo test -p tracewake-core --test event_schema_replay_gates` — event schema/replay lock layer; replay reconstructs projection.
3. `cargo test -p tracewake-core --test spine_conformance` — pipeline/spine seams map to named evidence.
4. Direct-dispatch negatives in `acceptance_gates`/`hidden_truth_gates`/`anti_regression_guards` fail closed.

### Invariants

1. Every world mutation in the witness has an appended event and replay ancestry (`INV-009`/`INV-011`).
2. Live and replay projection checksums agree within declared scope, or a divergence report is recorded (`INV-018`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs existing pipeline/replay/golden tests and the captured witness is the deliverable.`

### Commands

1. `cargo test -p tracewake-core --test event_schema_replay_gates`
2. `cargo test -p tracewake-content --test golden_fixtures_run && cargo test -p tracewake-core --test golden_scenarios`
3. `cargo test -p tracewake-core --test acceptance_gates` (direct-dispatch rejection boundary)
