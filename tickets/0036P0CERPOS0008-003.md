# 0036P0CERPOS0008-003: P0-02 evidence — autonomous decisions use sealed actor-known contexts with provenance, not validation or debug truth

**Status**: PENDING
**Priority**: HIGH
**Effort**: Medium
**Engine Changes**: None — fills the P0-02 section of the acceptance artifact from existing tests/fixtures
**Deps**: 0036P0CERPOS0008-001

## Problem

P0-CERT proof requirement 2 (spec §7.2): autonomous actor decisions must consume sealed actor-known contexts with provenance, never validation truth or debug truth. This ticket gathers the provenance rows, hidden-truth-resistance positives, and fail-closed negatives that prove no autonomous action is selected from hidden ground truth, and writes the P0-02 section of the acceptance artifact created by `-001`.

## Assumption Reassessment (2026-06-16)

1. The audited seams exist (verified 2026-06-16): `crates/tracewake-core/src/agent/{actor_known,no_human_surface,transaction,decision,candidate,perception,planner,htn,routine,methods,intention,trace}.rs`, `epistemics/{knowledge_context,knowledge_basis}.rs`, `checksum.rs`.
2. Spec §7.2 names the corpus; all named files exist at HEAD (confirmed this session). `stale_workplace_notice_superseded_by_newer_001.rs` — hedged "if exercised" in the spec — is present and folded into scope (freshness check). Artifact field shapes come from `-001` and `0003`.
3. Shared boundary under audit: the sealed actor-known context feeding action proposal generation (`INV-101`). Each autonomous proposal must pair with the event/projection ancestry that created the actor-visible belief, observation, memory, routine assignment, known route, or known affordance.
4. `INV-099` (truth may validate, not plan), `INV-100` (hidden-truth cognition forbidden), `INV-101` (actor-known context is sealed), `INV-102` (cognition inputs require provenance), and `INV-024` (no telepathy) motivate this ticket.
5. This ticket audits the truth-firewall / actor-knowledge surface as an evidence consumer. It runs the hidden-truth positives and the fail-closed negatives (`forbidden_provenance_input_fails_closed_001`, `hidden_truth_audit_rejects_typed_unproven_fact_without_banned_words_001`, `hidden_food_closed_container_001`, `hidden_route_edge_001`, `no_human_unseen_workplace_assignment_does_not_plan_work_001`) and records that hidden-truth audit/debug rows stay out of candidate-generation and planning inputs. It weakens no enforcement; a mutation supplying validation/debug truth to candidate generation or method selection must be killed (recorded `fail` if surviving).

## Architecture Check

1. Proving provenance per action-relevant fact (source kind, source event/projection/context IDs, freshness, holder, explicit-unknown representation) is stronger than asserting "the context looks sealed": a wrapper can carry truth-derived facts, so ancestry must be shown (spec §4.2/§7.2).
2. No backwards-compatibility aliasing or shims introduced.

## Verification Layers

1. `INV-100`/`INV-024` no hidden-truth cognition / no telepathy -> replay/golden-fixture check: `no_hidden_truth_planning_001`, `seeded_food_source_unknown_to_all_actors_001` show seeded facts stay unknown absent modeled knowledge.
2. `INV-102` provenance required -> schema/grep-proof: each action-relevant fact carries source kind + IDs + freshness; empty/dangling/wrong-kind/debug-only/validation-truth provenance fails closed (`forbidden_provenance_input_fails_closed_001`).
3. `INV-101` sealed context -> manual review + replay check: replay reconstructs the same context hash/frontier and proposal sequence for deterministic seeds.

## What to Change

### 1. Capture P0-02 provenance rows and hidden-truth-resistance witnesses

For each audited autonomous proposal, record the holder-known context ID/hash/frontier and, per action-relevant fact, source kind, source event/projection/context IDs, freshness/staleness classification, holder, and whether unknown was explicitly represented. Pair each proposal with the event/projection ancestry that created the actor-visible belief/observation/memory/assignment/known route/affordance.

### 2. Record fail-closed negatives and debug-quarantine evidence

Show the §7.2 negatives fail closed for `holder_known_context`/`candidate_generation`/`method_selection`/`local_planning`/`proposal_construction`, and that hidden-truth audit comparison rows and debug truth appear only in debug/review artifacts, absent from candidate-generation and planning inputs. Map failures to typed diagnostics with expected/actual input source and hidden-truth excluded/leaked.

## Files to Touch

- `archive/reports/0036_p0_cert_post_0008_baseline_certification_acceptance.md` (modify — file created by 0036P0CERPOS0008-001)

## Out of Scope

- The §8.10 command transcript and global fingerprints (owned by `-001`).
- Validation-truth boundary specifics (P0-06, owned by `-007`) and debug quarantine carrier census (P0-07, owned by `-008`) beyond what P0-02 requires.
- Remediation of any leakage found — recorded `fail`; fix routed to a separate `P0-CERT scoped remediation` ticket/spec.

## Acceptance Criteria

### Tests That Must Pass

1. `no_hidden_truth_planning_001` and `seeded_food_source_unknown_to_all_actors_001` — actor does not plan toward hidden truth.
2. `no_human_observation_facts_cite_log_events_001`, `no_human_workplace_knowledge_requires_notice_event_001`, `no_human_known_workplace_requires_provenance_001` — workplace/observation knowledge requires provenance.
3. Negatives fail closed: `forbidden_provenance_input_fails_closed_001`, `hidden_truth_audit_rejects_typed_unproven_fact_without_banned_words_001`, `hidden_food_closed_container_001`, `hidden_route_edge_001`, `no_human_unseen_workplace_assignment_does_not_plan_work_001`.

### Invariants

1. No autonomous action is selected from validation truth, raw fixture truth, debug panels, unproven schema data, display strings, or hidden-truth comparison rows (`INV-099`/`INV-100`).
2. Every action-relevant fact in a selected goal/plan carries replay-sufficient provenance; missing provenance fails closed (`INV-102`).

## Test Plan

### New/Modified Tests

1. `None — evidence-only ticket; verification runs the existing hidden-truth/actor-known fixtures and the captured provenance rows are the deliverable.`

### Commands

1. `cargo test -p tracewake-core --test hidden_truth_gates`
2. `cargo test -p tracewake-content --test golden_fixtures_run` (exercises the named hidden-truth and provenance fixtures)
3. `cargo test -p tracewake-core --test negative_fixture_runner` (fail-closed provenance negatives)
